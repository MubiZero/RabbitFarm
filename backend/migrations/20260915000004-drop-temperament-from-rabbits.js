'use strict';

/**
 * Убрать графу «характер» из карточки кролика.
 *
 * Колонка пришла из самой первой схемы (15 октября 2025) вместе со стартовой
 * таблицей и с тех пор жила только в схеме: поля для неё не было ни в форме,
 * ни в карточке, ни в отчётах — заполнить её было нельзя никаким способом.
 * Пустая графа, которую нельзя заполнить, — это не задел на будущее, а
 * обещание, которого никто не давал: следующий, кто откроет схему, потратит
 * время на вопрос «а почему она всегда пустая».
 *
 * Данных не теряем: заполнить её было нечем, поэтому она пуста у всех.
 * Откат возвращает колонку, но не содержимое — возвращать нечего.
 */
module.exports = {
  async up(queryInterface) {
    await queryInterface.removeColumn('rabbits', 'temperament');
  },

  async down(queryInterface, Sequelize) {
    await queryInterface.addColumn('rabbits', 'temperament', {
      type: Sequelize.STRING(100),
      allowNull: true
    });
  }
};
