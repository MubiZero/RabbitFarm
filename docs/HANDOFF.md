# Handoff: состояние на 2026-09-09 (обновлено)

Кратко — что сделано, что настроено локально и не попало в git, что дальше.
Пишется для продолжения работы с другого устройства.

## Что готово и закоммичено

- **MinIO** — файлы (фото кроликов) хранятся в S3-совместимом MinIO вместо
  локального диска, отдаются через прокси-роут API. Живая проверка через
  докер-стек пройдена (upload → fetch → delete → 404).
- **Эсхата Мерчант** — платёжный клиент (подпись SHA-256, dynamic-kassa,
  создание заказа + проверка статуса), модель `Payment` (tenant-scoped),
  сервис `paymentService` (webhook никогда не доверяется без перепроверки
  подписанным запросом к банку), контроллер, роуты. **Живьём проверено**
  против тестового контура банка (create + status — реальные ответы банка).
- **Payom SMS** — транспорт для шаблонных SMS, templateId берётся из
  конфига (`SMS_TEMPLATE_IDS`), не хардкодится. Постоянные ошибки
  (401/403/422) не ретраятся, временные (429/5xx/сеть) помечены retryable.
  **Реальная отправка подтверждена живьём** (2026-09-07): IP разработчика
  добавлен в вайтлист Payom напрямую (не через Tailscale — обычная сеть),
  SMS дошла на реальный номер, `deliveryStatus: SERVICE_ACCEPTED`. Шлюз
  требует номер строго в формате `+992XXXXXXXXX` (с `+`, 13 символов) —
  без плюса 422. Для повторной ручной проверки: `backend/scripts/test-payom-sms.js`.
- **Тесты**: 1200/1200 зелёных, 69/69 сьютов. Попутно найден и исправлен
  реальный кросс-платформенный баг — `testDb.js` дёргал
  `node_modules/.bin/sequelize-cli` (shell-shim), что падает с ENOENT на
  Windows и никогда не ловилось в CI (CI на ubuntu-latest). Теперь миграции
  тестовой БД запускаются через `node` напрямую по JS-файлу пакета.
- **Сброс пароля по коду (SMS/email)** — владелец фермы раньше не мог сам
  восстановить пароль (`resetMemberPassword` исключает роль `owner`), а
  единственный самостоятельный путь ничего никуда не отправлял. Теперь
  `forgotPassword`/`resetPassword` генерируют 6-значный код (не 32-байтный
  токен — тот было не набрать руками), шлют его по SMS (если есть телефон)
  или email (иначе), с блокировкой после 5 неверных попыток и сроком жизни
  15 минут. Email — через собственный SMTP (Stalwart, `mail.mubi.dev`),
  `backend/src/services/notifications/emailTransport.js` (nodemailer).
  Мобильный клиент: `/forgot-password` → `/reset-password`, кнопка вместо
  прежней статичной надписи на логине. **Живьём проверено** через реальный
  docker-стек (создание кода, инкремент попыток, блокировка после 5 раз),
  включая саму доставку email (2026-09-08): ящик `rabbitfarm@mubi.dev`
  заведён в Stalwart, письмо дошло, заголовки Authentication-Results в
  порядке (SPF/DKIM/DMARC pass). Домен `mubi.dev` уже имел автоматический
  DNS/ACME в Stalwart — отдельно поднимать не пришлось, нужен был только
  ящик. Для повторной ручной проверки: `backend/scripts/test-email.js`
  (см. «Известное ограничение окружения» насчёт Kaspersky/WSL).
  Попутно найден и исправлен реальный пробел в проде: `docker-compose.yml` и
  `docker-compose.coolify.yml` вообще не прокидывали `ESKHATA_*`/`SMS_*` в
  контейнер `api` — платежи и SMS были неработоспособны при любом деплое
  через docker-compose, хотя тестировались вручную мимо него.
- **Прод в Coolify обновлён и переведён на отдельные ресурсы (2026-09-08)**
  — прод-приложение `rabbitfarm` не передеплоивалось с ~20 августа: там не
  было ни MinIO, ни Firebase, ни Эсхаты/SMS/почты вообще. Сперва залатали
  на месте (залили `SMTP_*`/`ESKHATA_*`/`SMS_*`/`FIREBASE_*` в старый
  docker-compose-стек), но столкнулись с подтверждённым классом багов
  именно в Coolify для docker-compose + кастомный домен + нестандартный
  порт (открытые issue в coollabsio/coolify): Traefik роутил
  `api.rabbitfarm.mubi.dev` на порт 80 вместо 4567 несмотря на
  `SERVICE_FQDN_API_4567=/` в compose. По итогу решили не чинить это
  латкой, а перевести RabbitFarm на тот же паттерн, что уже у ManClient и
  imongroup на этом же Coolify — раздельные ресурсы вместо одного
  docker-compose-приложения:
  - `rabbitfarm-db` — Database (`standalone-mysql`), создан через
    `POST /api/v1/databases/mysql` (у Database-ресурсов сетевой доступ от
    других ресурсов работает из коробки, без доп. настройки).
  - `rabbitfarm-minio` — Service, создан через `POST /api/v1/services` с
    `docker_compose_raw` (base64), тем же шаблоном minio+create-bucket, что
    у `manclient-minio` — минио как one-click тип в Coolify не значится.
  - `rabbitfarm-api` / `rabbitfarm-web` — обычные Application
    (`POST /api/v1/applications/public`, build_pack `dockerfile`, НЕ
    compose) на GitHub-источнике «Public GitHub» (для приватного
    `private-github-app` без реального GitHub App instalation Coolify
    падает 500 — «Attempt to read property "private_key" on null»).
  - **Важное открытие**: у новых Application/Service по умолчанию
    `connect_to_docker_network: false` (в отличие от старых ресурсов
    ManClient, где `true`) — без этого флага контейнер не видит другие
    ресурсы по внутреннему docker-имени. Включается через
    `PATCH /api/v1/applications/{uuid}` (или `/services/{uuid}`) с
    `{"connect_to_docker_network": true}` + редеплой/рестарт.
  - Домен для обычного (не compose) Application задаётся полем `domains`
    (строка) через `PATCH /api/v1/applications/{uuid}` — без хаков с
    портом в URL, которые нужны только для docker-compose-ресурсов.
  - Данные в проде были только тестовые — мигрировать было нечего, стек
    подняли с чистого листа и переключили домены. Сквозная проверка
    (регистрация → создание кролика → загрузка фото в MinIO → раздача
    обратно, побайтово идентично) прошла на новых ресурсах, после чего
    домены `api.rabbitfarm.mubi.dev`/`rabbitfarm.mubi.dev` переключены со
    старого приложения `rabbitfarm` (uuid `7eyqlx3wysf5aljrdxja0nyz`) на
    новые. Старое приложение (вместе с volumes `db-data`/`minio-data`)
    удалено в этой же сессии сразу после проверки нового стека — данные
    были только тестовые, откат не понадобился.
  - Тестовая учётка `migration-test@example.com`/«Test Farm» после проверки
    удалена: `rabbitfarm-db` временно открывали публично
    (`is_public`/`public_port` через `PATCH /api/v1/databases/{uuid}` +
    restart), TRUNCATE всех таблиц кроме `SequelizeMeta` через `mysql2`
    напрямую, публичный порт закрыт обратно тем же способом. Прод сейчас
    полностью пустой (`ALLOW_REGISTRATION=false`, готов к первой реальной
    регистрации).

- **Тарифы и лимиты + платформенная админка, бэкенд (2026-09-08)** — первый
  кусок SaaS-логики поверх уже готовых `Payment`/`Farm`. Модель `Plan`
  (глобальная, не хозяйства — не входит в изоляцию ферм) с полями
  `max_rabbits`/`max_staff`/`price`/`is_active`, `farms.plan_id` (nullable,
  `ON DELETE SET NULL` — ферма без плана или после его удаления работает без
  ограничений). `planService.assertRabbitLimit`/`assertStaffLimit` проверяют
  лимит до создания записи: в `rabbitService.createRabbit` первой строкой (до
  открытия транзакции), в `staffService` — дважды, при выписке приглашения и
  повторно при его активации (приглашение живёт 7 дней, лимит могли добрать
  другим путём). Платформенный админ — не роль фермы, а флаг
  `users.is_platform_admin`, ставится вручную в БД; отдельный middleware
  `requirePlatformAdmin`. Новые эндпоинты `/api/v1/platform-admin/plans`
  (CRUD) и `/platform-admin/farms` (список всех ферм с фактическим
  потреблением + `PATCH /farms/:id/plan` для назначения тарифа) — see
  «Тарифы, лимиты и платформенная админка» в `ARCHITECTURE.md`. Юнит-тесты
  (842/842 зелёных) написаны и прогнаны; **миграции не прогонялись против
  живой БД** — локальный docker-стек в этой сессии не поднимался, интеграционные
  тесты (415) тоже не перепроверялись. Мобильного UI для админки нет —
  сделан только бэкенд, «вариант A — in-app суперадмин» на мобилке ещё
  предстоит.

## Настроено локально, НЕ в git (перенести на другое устройство вручную)

`backend/.env` (гитигнорится) содержит реальные тестовые креды:
- `ESKHATA_*` — тестовый контур банка (id организации, merchID, hash key).
- `SMS_API_TOKEN` — реальный токен Payom, `SMS_TEMPLATE_IDS` — маппинг
  `user.verification_code` → готовый templateId.

Актуальные плейсхолдеры и комментарии — в `backend/.env.example`. Значения
самих секретов у пользователя (не хранятся в репозитории намеренно).

## Известное ограничение окружения

Эсхата (`connecttest.eskhata.com`) банит доступ по IP с обычной сети
разработки — живая проверка сделана через Tailscale exit node (`honor-win`):
включить `tailscale set --exit-node=<name>`, выключить после
`tailscale set --exit-node=`. Payom (`gateway.payom.tj`) банил так же, но
решается проще — обычный вайтлист IP в кабинете Payom, без обхода через VPN.
IP разработчика похож на динамический (менялся в пределах одной сессии) —
если снова словите 403 с `IP-block`, вайтлист нужно обновить на актуальный.

Корпоративный Kaspersky Endpoint Security на рабочем Windows-ноуте блокирует
(рвёт TLS-хендшейк, `ECONNRESET`/`errno=10053`) любой трафик к почтовым
доменам напрямую с Windows — не помогает ни VPN, ни вайтлист IP в Stalwart
(IP не был забанен). Это блокировка на уровне антивируса, а не сети или
сервера. Обход: гонять SMTP-запросы из WSL2 (`wsl -d FedoraLinux-42 -- ...`)
— виртуальная сеть WSL2 не перехватывается тем же модулем антивируса,
TLS-хендшейк и живая отправка проходят нормально. Заодно выяснилось, что
порт 587 (STARTTLS) у `mail.mubi.dev` не отвечает вовсе (timeout) — рабочий
порт 465 (implicit TLS/SMTPS), см. `backend/src/config/mailer.js`.

## Дальше (по приоритету)

0. ~~Прогнать миграции тарифов/лимитов и интеграционные тесты на живой БД~~ —
   **закрыто (2026-09-08)**. Поднят докер (`db`+`minio`+`api`), миграции
   `20260908000002..000004` применены к `rabbitfarm_test` чисто. Интеграционные
   тесты: 427/434 зелёных — 7 упавших (`rabbit-gallery.test.js`,
   `farm-photo-feed.test.js`) не связаны с тарифами/лимитами: MinIO
   намеренно не публикует порт 9000 наружу (см. `docker-compose.yml`,
   комментарий у сервиса `minio`) — работает только `api`-контейнер по
   внутренней сети, а тесты бьют MinIO с хоста напрямую. Известное
   ограничение окружения для запуска интеграционных тестов **с хоста**, не
   баг в коде; чтобы гонять фото-тесты, нужно либо временно опубликовать
   9000, либо запускать тесты внутри `api`-контейнера.
1. ~~Согласовать с банком реальный формат webhook-пейлоада перед боем~~ —
   **закрыто (2026-09-08)**: получена официальная спека Эсхата-Мерчант
   (раздел 9, Webhook). Реальный формат — `{ status, code, message, data:
   { orderId, orderStatus, invoiceId, amount, currency, posId } }`, поля
   заказа вложены в `data`, а не на верхнем уровне (наше прежнее
   предположение было неверным). Вебхук **не подписан** (без `hash`) — это
   подтверждает, что архитектура «никогда не доверять вебхуку без
   перепроверки подписанным `/orders/status`» была правильной с самого
   начала. Поправлено: `paymentController.webhook` читает
   `req.body.data.invoiceId` вместо `req.body.invoiceId`, тесты обновлены
   под вложенный формат.
2. ~~Следующий крупный блок SaaS: Планы/лимиты + Платформенная админка~~ —
   **закрыто целиком (2026-09-08)**: бэкенд проверен на живой БД (см. выше),
   мобильный UI сделан — `mobile/lib/features/platform_admin/`, вариант A
   (in-app суперадмин). Экран «Платформа» с двумя вкладками: «Фермы»
   (владелец, тариф, потребление полосой против предела, предупреждение при
   подходе к пределу и при упоре в него, назначение/снятие тарифа) и
   «Тарифы» (создание, правка, удаление). Вход — раздел «Платформа» на
   вкладке «Хозяйство», только при `is_platform_admin`. Попутно:
   - список ферм теперь отдаёт владельца (`full_name`/`email`/`phone`) —
     раньше был только `owner_id`, и админ не понимал, чья это ферма;
   - `PATCH /farms/:id/plan` отвечает фермой той же формы, что элемент
     списка (с потреблением) — клиент обновляет одну карточку вместо
     перечитывания страницы;
   - `plans.is_active` перестал быть флагом без последствий: выключенный
     тариф больше нельзя назначить (`PLAN_INACTIVE` → 400), у ферм, где он
     уже стоит, остаётся.

   Тесты: бэкенд 846/846 юнит, мобилка 227/227 (+19 новых).

   Продуктовое решение «что видит сама ферма при упоре в лимит» закрыто
   ниже, в пункте 5.
3. ~~Валюта в приложении — рубли, а сервис для Таджикистана~~ — **закрыто
   (2026-09-08)**. Знак валюты был вписан руками в четырёх несвязанных
   местах, и во всех четырёх стоял рубль: `formatMoney`, суффикс суммы
   операции, суффикс цены корма и подпись «Затраты, ₽» прямо в словаре.
   Бэкенд при этом был прав всегда — Эсхата получает `currency = '972'`
   (ISO-код сомони), и `Payment.currency` по умолчанию тоже `972`; врал
   только интерфейс. Теперь знак — одна константа `kCurrencySymbol` в
   `mobile/lib/core/utils/format_utils.dart` («с»), формы и словарь берут
   её оттуда (`medFormCostLabel` получает знак параметром, а не зашивает в
   перевод). Заодно у цены тарифа появился суффикс валюты — у суммы
   операции и цены корма он был, у неё нет. Пины: `test/utils/
   format_utils_test.dart` и `test/l10n/labels_test.dart`.

4. Бэклог (осознанно отложено): бэкап, мониторинг; фолбэк «SMS не ушла →
   пробуем email» в сбросе пароля; per-account cooldown на повторный запрос
   кода; Таджикистан-специфичный regex телефона (`+992`) вместо общего
   международного. Масштабирование — только как варианты на обсуждение, без
   реализации (MinIO — единственный уже сделанный "дешёвый сейчас" шаг).
5. ~~Этап 1 плана `docs/plans/PLATFORM-ADMIN.md` («Фундамент»)~~ — **закрыт
   целиком (2026-09-08)**, шесть задач сделаны параллельными агентами и
   смержены по одной с прогоном тестов после каждого мержа:
   - **1.1 Журнал действий админа** — таблица `admin_audit_log`,
     `auditService.record()`/`list()`; мутирующие `platform-admin`-эндпоинты
     (тарифы, назначение плана) пишут в журнал вместо `logger.info`. Новый
     `GET /platform-admin/audit`.
   - **1.2 Тариф по умолчанию и срок действия** — `plans.is_default` (ровно
     один, второй отклоняется явно — `DEFAULT_PLAN_EXISTS`, не переключает
     молча), `farms.plan_expires_at` (пусто = бессрочно). Регистрация
     назначает ферме дефолтный тариф. Мобилка: переключатель «выдавать новым
     фермам» в форме тарифа.
   - **1.3 Предупреждение о лимите для самой фермы** — `GET
     /reports/dashboard` отдаёт `plan_usage`; на «Сегодня» полоса «26 из 30»
     при подходе к пределу (≥80%); понятный диалог вместо текста ошибки при
     самом упоре (создание кролика, приглашение сотрудника). Попутно:
     `ApiResponse.badRequest` начал пробрасывать машинный код ошибки
     (`RABBIT_LIMIT_REACHED`/`STAFF_LIMIT_REACHED`) — раньше терялся за
     общим `BAD_REQUEST`.
   - **1.4 Факты о ферме** — `GET /platform-admin/farms/:id` на уже
     существовавший `platformAdminService.getFarm`; добавлена последняя
     активность (`max(users.last_login_at)`).
   - **1.5 Поиск/фильтры/сортировка списка ферм** — поиск по названию/
     владельцу, фильтры `no_plan`/`at_limit`/`inactive_days`, сортировка по
     потреблению/активности/дате. Осознанно посчитано в JS после одного
     запроса, а не коррелированными подзапросами в SQL — платформа про
     десятки ферм, не про миллион строк. Фильтры «приостановлена»/
     «просрочена» из текста плана пока не реализованы — первый ждёт
     `farms.status` (шаг 2.2), второй технически готов (`plan_expires_at` из
     1.2 уже есть) и годится для быстрой доделки.
   - **1.6 Учёт занятого места** — `photos.size_bytes` /
     `rabbits.photo_size_bytes`, пишутся при загрузке из `req.file.size` без
     похода в MinIO. Новые ключи объектов — `farm-<id>/<folder>/...` вместо
     `<folder>/...` (старые URL остаются валидными как есть — это же
     понадобится для чистки файлов при удалении фермы, шаг 2.4). Бэкфилл
     размера для существующих записей — `backend/scripts/
     backfillPhotoSizes.js` (не запускался против боевого MinIO — нет
     доступа из среды разработки, но идемпотентен и безопасен для запуска
     когда понадобится).

   Тесты: бэкенд 900/900 юнит, мобилка 244/244, `flutter analyze` чисто.
   Живой БД в среде разработки не было — миграции не прогонялись, только
   написаны в стиле соседних; прогнать `npm run db:migrate` и живой смоук
   перед тем, как этап 2 начнёт полагаться на новые колонки, будет не лишним
   (как это уже делалось для тарифов в пункте выше).

   Живой БД и интеграционных тестов ждало — **закрыто (2026-09-09)**: поднят
   докер (`db`+`minio`+`api`), `20260908000008` прогнана против
   `rabbitfarm_test` чисто, интеграционные тесты 427/434 (те же 7 падений
   про MinIO с хоста, что и раньше, — не регресс).

6. ~~Этап 2 плана `docs/plans/PLATFORM-ADMIN.md` («Рычаги админа»)~~ —
   **закрыт целиком (2026-09-09)**, вместе с хвостом 1.5 (фильтры
   `suspended`/`expired` — ждали `farms.status` из 2.2):
   - **2.1 Карточка фермы** — `GET /platform-admin/farms/:id` расширен
     составом (роли, `last_login_at`), последними 20 платежами (без
     `raw_response` — банковский сырой ответ незачем гонять в ответ на
     просмотр карточки клиента) и занятым местом в MinIO
     (`photos.size_bytes` + `rabbits.photo_size_bytes`). Мобильный экран
     «Карточка фермы» — все секции плюс кнопки действий из 2.2/2.3.
   - **2.2 Приостановка и возврат** — `farms.status`
     (`active`/`read_only`/`suspended`), проверяется в единственной
     точке — `authenticate` (`FARM_SUSPENDED`/`FARM_READ_ONLY`), а не по
     контроллерам. Платформенный админ не блокирует сам себя статусом
     собственной фермы. Мобилка — лист выбора статуса с подтверждением.
   - **2.3 Разовые поблажки** — `farms.extra_rabbits`/`extra_staff`/
     `extras_until`, входят во все проверки лимита через
     `planService.getEffectiveLimit` (истёкшая поблажка не считается).
     Мобилка — диалог выдачи/снятия.
   - **2.4 Экспорт и удаление** — `GET /farms/:id/export` (один JSON,
     синхронно: staff, кролики со всей историей, платежи без
     `raw_response` — масштаб десятки ферм, не миллионы записей, очередь
     не нужна). Мягкое удаление — не Sequelize `paranoid` (тихо
     подключил бы скоуп ко всем запросам `Farm` по всему коду), а простое
     `farms.deleted_at`, проверяемое явно в `authenticate` (доступ
     закрывается сразу) и в `listFarms` (по умолчанию не показывает
     удалённые). Двойное подтверждение — набрать точное название фермы,
     перепроверяется и на сервере (`CONFIRM_NAME_MISMATCH`), не только в
     мобильной форме. Физическая зачистка — `jobs/farmPurgeJob` раз в
     сутки, старше 30 дней. **Находка, проверенная живьём**: каскада
     `Farm.hasMany(..., onDelete: 'CASCADE')` для физического удаления не
     хватает — внутри фермы есть встречные `RESTRICT`-ключи
     (`rabbits.breed_id`, `breedings.male_id`/`female_id`,
     `births.mother_id`, `feeding_records.feed_id`), и `DELETE FROM farms`
     падает, как только у фермы есть хоть один кролик. `RESTRICT` менять
     на `CASCADE` нельзя (это защита при удалении *одной* записи, не
     фермы целиком) — вместо этого `purgeFarm` сносит мешающие таблицы
     сам, в правильном порядке, перед удалением фермы. Проверено живьём
     дважды (агентом и повторно вручную) на живой БД, включая кролика с
     самоссылками `father_id`/`mother_id` — зачистка проходит и через
     self-reference. Мобильный экспорт — просмотр и копирование JSON, не
     сохранение/шаринг файла: в приложении нет `share_plus`/
     `path_provider`, тянуть их ради одной кнопки в админке — решение
     уровня архитектуры, а не одного экрана (аудитория — один
     платформенный админ, ему достаточно вставить текст куда нужно).

   Тесты: бэкенд 980/980 юнит, мобилка 278/278, `flutter analyze` чисто.
   Миграция `20260909000001` (`farms.deleted_at`) и вся физическая зачистка
   проверены живьём на `rabbitfarm_test`.

   Известный, осознанно не закрытый край: `admin_audit_log.admin_id →
   users` — `NO ACTION`. Если когда-нибудь понадобится физически удалить
   ферму самого платформенного админа (а не ферму клиента), `purgeFarm`
   упрётся в эту связь — сценарий вырожденный, чинить не стали.

   Следующий шаг по плану — этап 3 («Работа с клиентом»): объявления,
   вход под клиентом только для чтения.

7. ~~Этап 3.1 плана `docs/plans/PLATFORM-ADMIN.md` («Объявления»)~~ —
   **закрыт и закоммичен** (2026-09-09). Рассылка push/email
   всем фермам, одной ферме или по фильтру списка ферм (переиспользован тот же
   фильтр, что в 1.5). **SMS сознательно не входит**: шлюз Payom принимает
   только заранее одобренные шаблоны, свободный текст объявления через него
   не отправить технически — понадобится, заводить отдельный шаблон и путь
   отправки отдельной задачей. `Announcement` — глобальная сущность (как
   `Plan`/`AdminAuditLog`), `announcementService.create` шлёт синхронно и
   отклоняет `NO_RECIPIENTS`, если получателей не нашлось (не пишет в историю
   отправку в никуда). Мобилка — третья вкладка «Объявления» с формой
   составления, подтверждением перед отправкой и историей с доставкой по
   каналам. Тесты: бэкенд 1033/1033 юнит (+50 новых), мобилка 287/287,
   `flutter analyze` чисто.

   Продуктовые вопросы, оставленные как есть (не додумывались):
   полный текст объявления не открыть из истории (обрезан до двух строк —
   нет экрана/листа просмотра); отправка при нуле получателей — сейчас это
   `NO_RECIPIENTS` (400), а не тихий успех.

   **Побочный, но обязательный ремонт: апгрейд генераторов кода мобилки.**
   Пытаясь просто перегенерировать модели под новый класс `Announcement`,
   упёрлись в то, что `build_runner` вообще не мог сделать холодную сборку в
   этом окружении: пакет `analyzer` (7.6.0, тянется старыми `freezed`
   `^2.4.6`/`riverpod_generator` `^2.3.9`) падает `visitDotShorthandPropertyAccess`
   на новом синтаксисе Dart SDK 3.13 при полном (не инкрементальном) билде —
   деталь, из-за которой воспроизвести баг стабильно получилось только после
   того, как несколько прерванных попыток стёрли валидный asset graph и
   заставили билдер собираться с нуля. Точечный `dependency_overrides` на
   один `analyzer` не работает — ломает API `build_resolvers`. Решение —
   `flutter pub upgrade --major-versions` по всей связке кодогенерации разом:
   `build_runner` 2.16.1, `freezed` 4.0.1, `freezed_annotation` 3.1.0,
   `json_serializable` 6.14.1, `json_annotation` 4.12.0, `riverpod_generator`
   4.0.9, `riverpod_annotation` 4.0.7, `flutter_riverpod` 3.4.3 (тянет
   `riverpod` 3.4.3, `analyzer` 14.3.0). Следствия для всего мобильного кода,
   не только объявлений:
   - **freezed 3+/4 требует `abstract class X with _$X`**, а не просто
     `class X with _$X` — иначе «Missing concrete implementations». Правлено
     механической заменой во всех 22 файлах моделей проекта (плюс два
     generic-класса `ApiResponse<T>`/`PaginatedResponse<T>` отдельно — их не
     ловил простой regex без `<T>`).
   - **`StateNotifier`/`StateNotifierProvider` никуда не делись**, просто
     переехали в отдельный энтрипойнт `package:flutter_riverpod/legacy.dart`
     — во всех 15 файлов с ручными `StateNotifier`-провайдерами (включая
     `platform_admin_provider.dart`) добавлен этот импорт, сама логика не
     тронута. Ручной (не codegen) family+autoDispose `AsyncNotifier`
     (`PlatformFarmDetailNotifier`, `TodayTasksNotifier`) в новой версии
     называется просто `AsyncNotifier`/убрал `AutoDispose`-префикс из имени
     базового класса — `PlatformFarmDetailNotifier` при этом переведён на тот
     же `StateNotifier`-путь (у ручного family-нотифаера без кодогенерации
     нет прямого аналога прежнего `arg`-геттера), важно: наблюдение за
     `platformAdminRepositoryProvider` обязано остаться через `ref.watch` на
     уровне фабрики провайдера (не `ref.read` внутри конструктора нотифаера)
     — иначе смена репозитория (в тестах через override) не пересоздаёт
     нотифаер, и карточка остаётся на старых данных.
   - **`AsyncValue.valueOrNull` переименован в `.value`** (сам `.value`
     стал nullable-геттером, старый бросающий `.value` теперь `.requireValue`)
     — заменено везде одним `sed`.
   - **`Override` (тип для `ProviderScope.overrides`) переехал** в
     `package:flutter_riverpod/misc.dart` — добавлен импорт в 7 тестовых
     файлов, где он назван явно.
   - **Riverpod 3.x сам молча ретраит упавший провайдер** (до 10 раз,
     экспоненциальная пауза 200мс→6.4с) — пока идёт ретрай, `AsyncValue`
     держит и ошибку, и признак загрузки одновременно, и `.when()` в этом
     окне отдаёт `loading`, а не `error`. У приложения уже есть свой ответ на
     сбой — явная кнопка «Повторить» на каждом экране, — поэтому авторетрай
     отключён глобально: `retry: (_, __) => null` в корневом `ProviderScope`
     (`lib/main.dart`) и в тестовом `testApp`/`testAppScreen`
     (`test/support/test_app.dart`). Без этого несколько тестов «на ошибке
     предлагает повторить» виснут на фиктивной загрузке дольше, чем длится
     сам тест.
   - `CupertinoPageTransitionsBuilder` в `lib/core/theme/app_theme.dart` не
     резолвился без явного `import 'package:flutter/cupertino.dart'` —
     самостоятельный баг, не связанный с апгрейдом (транзитивный экспорт из
     `material.dart` в этой версии SDK на него не распространяется), попутно
     найден и починен при прогоне `flutter analyze`.

   Итог: `flutter analyze` чисто, `flutter test` 287/287 — диф огромный
   (все `.freezed.dart`/`.g.dart` перегенерированы, `abstract` добавлен в 22
   файлах, `legacy.dart` — в 15), закоммичен отдельно от фичи объявлений
   (`chore(mobile): апгрейд кодогенерации...`), диффы иначе были бы
   неразличимы.

8. ~~Этап 3.2 плана `docs/plans/PLATFORM-ADMIN.md` («Вход под клиентом,
   только чтение»)~~ — **закрыт и закоммичен** (2026-09-09). Тем самым
   **этап 3 плана закрыт целиком**.
   - `POST /platform-admin/farms/:id/impersonate`: короткоживущий (15 мин,
     тот же срок, что у обычного access-токена) токен на владельца фермы с
     claim'ами `read_only`/`impersonated_by`. Отдельный refresh не
     выпускается — истёк срок, значит заново через админку.
   - Проверка — в `authenticate`, той же единственной точке, что статус
     фермы (2.2): `read_only` в токене режет все методы кроме
     GET/HEAD/OPTIONS и **проверяется раньше статуса фермы** — админ может
     просмотреть даже приостановленную/read-only ферму, это и есть цель.
   - Причина обязательна (`impersonateFarmSchema`, Joi) и уходит в журнал
     вместе с id фермы и владельца — иначе в логе не отличить осмысленный
     вход от «зашёл посмотреть от скуки».
   - Мобилка: диалог запроса причины на карточке фермы, баннер режима
     просмотра на всё время сеанса, восстановление обычной сессии админа
     при выходе (обычный logout стирает и отложенную сессию просмотра, не
     только текущую — иначе токены админа зависают в хранилище).

   Тесты: бэкенд 1045/1045 юнит (+12 новых), мобилка зелёные (включая
   отдельный `test/auth/impersonation_test.dart`), `flutter analyze` чисто.

9. ~~Этап 4.1 плана `docs/plans/PLATFORM-ADMIN.md` («Связка тарифа с
   оплатой»)~~ — **закрыт и закоммичен** (2026-09-09).
   - Сумму платежа считает сервер по тарифу фермы
     (`planService.getRenewalQuote`), а не тело запроса — раньше
     `POST /payments` принимал произвольную сумму от клиента, что позволяло
     заплатить сколько угодно за свой же тариф. `NO_PLAN`/`PLAN_FREE`
     отклоняют оплату, если платить не за что.
   - Продление — `planService.extendPlanExpiry`: от текущего срока, если он
     не истёк, иначе от «сейчас», плюс фиксированные 30 дней (константа, не
     календарный месяц — минимум вместо аппарата пропорционального
     пересчёта). Срабатывает и по вебхуку банка, и по новому
     `GET /payments/:invoiceId` — ручному опросу для мобилки, которая
     вебхук не видит вовсе.
   - Ничего не продлевает, если у фермы уже нет платного тарифа на момент
     подтверждения (админ мог переключить план, пока платёж висел) — это
     логируется, а не продлевает молча не тот тариф.
   - Админ может продлить вручную (`PATCH
     .../farms/:id/plan-expiry`, точная дата, как и `extras_until`) — с
     записью в журнал (`farm.extend_plan`).
   - Мобилка: новый экран «Тариф» (`/subscription`, пункт в «Настройках» у
     владельца) — название, цена, срок, кнопка «Оплатить»; после создания
     заказа — ссылка на оплату банка (`url_launcher`, новая зависимость) и
     кнопка «Проверить оплату». На карточке фермы в админке — кнопка
     «Продлить вручную» рядом со сроком тарифа.
   - `planService.getUsage` (уже используется в `/reports/dashboard`)
     расширен полем `plan` — переиспользован существующий дашборд вместо
     отдельного эндпоинта для экрана «Тариф».

   Тесты: бэкенд 1062/1062 юнит (+17 новых), мобилка 307/307 (+11 новых),
   `flutter analyze` чисто.

10. ~~Этап 4.2 плана `docs/plans/PLATFORM-ADMIN.md` («Напоминания о
    продлении»)~~ — **закрыт и закоммичен** (2026-09-09). Тем самым **этап 4
    плана закрыт целиком**.
    - `jobs/planExpiryReminderJob.js`, тот же час, что у
      `notificationDigestJob` (08:00 сервера). За 7 и за 1 день до
      `plan_expires_at` — push и письмо владельцам фермы; в день истечения
      (`days <= 0`, не строго `=== 0` — самовосстанавливается, если день
      был пропущен) активная ферма переводится в `read_only` с
      уведомлением. Приостановленную админом ферму не трогает и не
      снимает; уже `read_only` — не повторяет.
    - Ферма без известного срока (бесплатный тариф по умолчанию,
      `plan_expires_at = null`) в выборку не попадает вовсе.

    Тесты: бэкенд 1075/1075 юнит (+13 новых).

    **Найденный по ходу пробел (не в рамках 4.2):** у самой фермы нет
    экрана/баннера «доступ только для чтения» — 2.2 построил только сторону
    админа (выбор статуса), а баннер для фермы, заявленный в исходном плане
    2.2, так и не появился. Закрыт следующим пунктом.

11. ~~Пробел из 2.2 — баннер «доступ только для чтения» у самой фермы~~ —
    **закрыт и закоммичен** (2026-09-09).
    - Бэкенд: `authService.getProfile`/`login` теперь тянут `farm.status`
      вместе с профилем (`include`, та же связка, что уже была в
      `middleware/auth.js` для проверки доступа, — просто раньше не
      возвращалась клиенту). `register` проставляет его руками: свежая
      ферма создаётся без `include`, а статус у неё всегда `active`.
    - Мобилка: постоянный баннер на всё приложение, тем же способом, что и
      плашка входа под клиентом (оборачивает роутер в `main.dart`) — два
      источника сразу:
      - **проактивно** — из `authProvider.user.farm.status` при каждом
        обновлении профиля (холодный старт, `refreshProfile`);
      - **реактивно** — новый колбэк `ApiClient.onFarmAccessChanged`
        (`ErrorInterceptor` разбирает код `FARM_READ_ONLY`/`FARM_SUSPENDED`
        на ответе ЛЮБОГО запроса, не только профиля) — иначе ферма,
        приостановленная админом посреди уже открытого сеанса, узнала бы
        об этом только на следующем перезапуске.
      - У `read_only` — кнопка «Тариф» (`/subscription`), кроме просмотра
        под клиентом: там и так всё только для чтения через JWT-claim,
        кнопка вела бы в тупик.
    - `farm_status_labels.dart` (подписи, цвета, иконки статусов) переехал
      из `platform_admin` в `core/utils` — общий словарь для карточки
      админа и баннера самой фермы, вместо двух копий или обратной
      зависимости фичи `auth` на `platform_admin`.

    Тесты: бэкенд 1077/1077 юнит (+3 новых), мобилка 318/318 (+11 новых),
    `flutter analyze` чисто.

    Следующий шаг по плану — этап 5 (сводка платформы).
