# RabbitFarm: Quality to 10/10 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Поднять все метрики качества проекта до 10/10 — тесты, безопасность, документация, код, функциональность.

**Architecture:** Последовательно закрываем каждый пробел: сначала тестирование (самый критичный Gap), затем безопасность, функциональность, документация и чистота кода. Каждый таск — атомарный коммит.

**Tech Stack:** Node.js/Express, Sequelize/MySQL, Jest/Supertest, swagger-jsdoc/swagger-ui-express, Flutter/Dart

---

## Текущие оценки и цели

| Критерий | Сейчас | Цель | Главный пробел |
|---------|--------|------|---------------|
| Функциональность | 9/10 | 10/10 | Нет сброса пароля |
| Качество кода | 8/10 | 10/10 | Дублирование логики, нет сервисов для части контроллеров |
| Безопасность | 8/10 | 10/10 | Logout не инвалидирует access token, секреты в docker-compose |
| Поддерживаемость | 8/10 | 10/10 | Нет крон-очистки токенов, нет env-валидации |
| Покрытие тестами | 1/10 | 10/10 | Нет ни одного теста |
| Документация | 7/10 | 10/10 | Нет Swagger/OpenAPI |

---

## ФАЗА 1: ТЕСТИРОВАНИЕ (критично)

### Task 1: Настройка тестовой инфраструктуры

**Files:**
- Create: `backend/jest.config.js`
- Create: `backend/tests/setup.js`
- Create: `backend/tests/helpers/mockModels.js`
- Modify: `backend/package.json`

**Step 1: Создать конфиг Jest**

```js
// backend/jest.config.js
module.exports = {
  testEnvironment: 'node',
  testMatch: ['**/tests/**/*.test.js'],
  setupFilesAfterFramework: ['./tests/setup.js'],
  coverageDirectory: 'coverage',
  collectCoverageFrom: ['src/**/*.js', '!src/seeders/**'],
  coverageThreshold: {
    global: { branches: 70, functions: 80, lines: 80, statements: 80 }
  },
  testTimeout: 10000
};
```

**Step 2: Создать setup файл**

```js
// backend/tests/setup.js
process.env.NODE_ENV = 'test';
process.env.JWT_SECRET = 'test_secret_key_minimum_32_chars_long';
process.env.JWT_REFRESH_SECRET = 'test_refresh_secret_key_minimum_32_chars';
process.env.JWT_EXPIRE = '15m';
process.env.JWT_REFRESH_EXPIRE = '7d';

// Заглушаем логгер чтобы не засорять вывод тестов
jest.mock('../src/utils/logger', () => ({
  info: jest.fn(),
  error: jest.fn(),
  warn: jest.fn(),
  debug: jest.fn()
}));
```

**Step 3: Создать хелперы моков моделей**

```js
// backend/tests/helpers/mockModels.js
const createMockUser = (overrides = {}) => ({
  id: 1,
  email: 'test@example.com',
  password_hash: '$2b$10$hashedpassword',
  full_name: 'Test User',
  role: 'owner',
  is_active: true,
  last_login_at: null,
  toJSON: function() { return { ...this }; },
  update: jest.fn().mockResolvedValue(true),
  ...overrides
});

const createMockRabbit = (overrides = {}) => ({
  id: 1,
  name: 'Буся',
  user_id: 1,
  breed_id: 1,
  sex: 'female',
  birth_date: '2024-01-01',
  status: 'healthy',
  cage_id: null,
  tag_id: 'TAG001',
  toJSON: function() { return { ...this }; },
  update: jest.fn().mockResolvedValue(true),
  destroy: jest.fn().mockResolvedValue(true),
  ...overrides
});

module.exports = { createMockUser, createMockRabbit };
```

**Step 4: Добавить скрипт в package.json**

В `backend/package.json` скрипты уже есть (`"test": "jest --coverage"`). Добавить:
```json
"test:unit": "jest tests/unit --coverage",
"test:integration": "jest tests/integration --coverage"
```

**Step 5: Запустить тесты (пока нет файлов — убедиться что Jest запускается)**
```bash
cd backend && npx jest --listTests
```
Ожидаемо: пустой список, без ошибок.

**Step 6: Commit**
```bash
git add backend/jest.config.js backend/tests/setup.js backend/tests/helpers/mockModels.js backend/package.json
git commit -m "test: add jest configuration and test helpers"
```

---

### Task 2: Unit-тесты для утилит

**Files:**
- Create: `backend/tests/unit/utils/jwt.test.js`
- Create: `backend/tests/unit/utils/password.test.js`

**Step 1: Написать тесты для JWT-утилит**

```js
// backend/tests/unit/utils/jwt.test.js
const JWTUtil = require('../../../src/utils/jwt');

describe('JWTUtil', () => {
  const payload = { id: 1, email: 'test@example.com', role: 'owner' };

  describe('generateAccessToken', () => {
    it('должен создавать валидный access token', () => {
      const token = JWTUtil.generateAccessToken(payload);
      expect(token).toBeDefined();
      expect(typeof token).toBe('string');
      expect(token.split('.')).toHaveLength(3);
    });
  });

  describe('verifyAccessToken', () => {
    it('должен верифицировать корректный token', () => {
      const token = JWTUtil.generateAccessToken(payload);
      const decoded = JWTUtil.verifyAccessToken(token);
      expect(decoded.id).toBe(payload.id);
      expect(decoded.email).toBe(payload.email);
      expect(decoded.role).toBe(payload.role);
    });

    it('должен бросать ошибку для невалидного token', () => {
      expect(() => JWTUtil.verifyAccessToken('invalid.token.here')).toThrow('Invalid or expired token');
    });

    it('должен бросать ошибку для подделанного token', () => {
      const token = JWTUtil.generateAccessToken(payload);
      const tampered = token.slice(0, -5) + 'XXXXX';
      expect(() => JWTUtil.verifyAccessToken(tampered)).toThrow('Invalid or expired token');
    });
  });

  describe('generateRefreshToken + verifyRefreshToken', () => {
    it('должен создавать и верифицировать refresh token', () => {
      const refreshPayload = { id: 1 };
      const token = JWTUtil.generateRefreshToken(refreshPayload);
      const decoded = JWTUtil.verifyRefreshToken(token);
      expect(decoded.id).toBe(1);
    });
  });

  describe('decode', () => {
    it('должен декодировать token без верификации', () => {
      const token = JWTUtil.generateAccessToken(payload);
      const decoded = JWTUtil.decode(token);
      expect(decoded.id).toBe(payload.id);
    });
  });
});
```

**Step 2: Написать тесты для утилиты паролей**

```js
// backend/tests/unit/utils/password.test.js
const PasswordUtil = require('../../../src/utils/password');

describe('PasswordUtil', () => {
  describe('hash', () => {
    it('должен хешировать пароль', async () => {
      const hash = await PasswordUtil.hash('mypassword123');
      expect(hash).toBeDefined();
      expect(hash).not.toBe('mypassword123');
      expect(hash.startsWith('$2b$')).toBe(true);
    });

    it('должен создавать разные хеши для одного пароля', async () => {
      const hash1 = await PasswordUtil.hash('mypassword123');
      const hash2 = await PasswordUtil.hash('mypassword123');
      expect(hash1).not.toBe(hash2);
    });
  });

  describe('compare', () => {
    it('должен возвращать true для верного пароля', async () => {
      const hash = await PasswordUtil.hash('correctpassword');
      const result = await PasswordUtil.compare('correctpassword', hash);
      expect(result).toBe(true);
    });

    it('должен возвращать false для неверного пароля', async () => {
      const hash = await PasswordUtil.hash('correctpassword');
      const result = await PasswordUtil.compare('wrongpassword', hash);
      expect(result).toBe(false);
    });
  });
});
```

**Step 3: Запустить тесты**
```bash
cd backend && npx jest tests/unit/utils --verbose
```
Ожидаемо: все тесты PASS.

**Step 4: Commit**
```bash
git add backend/tests/unit/
git commit -m "test: add unit tests for JWT and password utilities"
```

---

### Task 3: Unit-тесты для AuthService

**Files:**
- Create: `backend/tests/unit/services/authService.test.js`

**Step 1: Написать тест файл для AuthService**

```js
// backend/tests/unit/services/authService.test.js
jest.mock('../../../src/models', () => ({
  User: {
    findOne: jest.fn(),
    findByPk: jest.fn(),
    create: jest.fn(),
    sequelize: { transaction: jest.fn() }
  },
  RefreshToken: {
    create: jest.fn(),
    findOne: jest.fn(),
    destroy: jest.fn()
  }
}));

const { User, RefreshToken } = require('../../../src/models');
const authService = require('../../../src/services/authService');
const { createMockUser } = require('../../helpers/mockModels');

describe('AuthService', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('login', () => {
    it('должен успешно логинить пользователя с верными данными', async () => {
      const mockUser = createMockUser();
      // bcrypt.compare возвращает true для правильного пароля
      // Используем реальный хеш 'password123'
      mockUser.password_hash = '$2b$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy';
      mockUser.update = jest.fn().mockResolvedValue(true);
      User.findOne.mockResolvedValue(mockUser);
      RefreshToken.create.mockResolvedValue({});

      const result = await authService.login('test@example.com', 'password123');

      expect(result).toHaveProperty('access_token');
      expect(result).toHaveProperty('refresh_token');
      expect(result).toHaveProperty('user');
      expect(User.findOne).toHaveBeenCalledWith({ where: { email: 'test@example.com' } });
    });

    it('должен бросать INVALID_CREDENTIALS если пользователь не найден', async () => {
      User.findOne.mockResolvedValue(null);

      await expect(authService.login('notexist@example.com', 'password'))
        .rejects.toThrow('INVALID_CREDENTIALS');
    });

    it('должен бросать USER_INACTIVE если аккаунт неактивен', async () => {
      User.findOne.mockResolvedValue(createMockUser({ is_active: false }));

      await expect(authService.login('test@example.com', 'password'))
        .rejects.toThrow('USER_INACTIVE');
    });

    it('должен бросать INVALID_CREDENTIALS при неверном пароле', async () => {
      const mockUser = createMockUser({ password_hash: 'wronghash' });
      User.findOne.mockResolvedValue(mockUser);

      await expect(authService.login('test@example.com', 'wrongpassword'))
        .rejects.toThrow('INVALID_CREDENTIALS');
    });
  });

  describe('register', () => {
    it('должен бросать USER_EXISTS если email занят', async () => {
      const mockTransaction = {
        commit: jest.fn(),
        rollback: jest.fn()
      };
      User.sequelize.transaction.mockResolvedValue(mockTransaction);
      User.findOne.mockResolvedValue(createMockUser());

      await expect(authService.register({
        email: 'test@example.com',
        password: 'password123',
        full_name: 'Test User'
      })).rejects.toThrow('USER_EXISTS');

      expect(mockTransaction.rollback).toHaveBeenCalled();
    });
  });

  describe('logout', () => {
    it('должен удалять refresh token из БД', async () => {
      RefreshToken.destroy.mockResolvedValue(1);
      const result = await authService.logout('some-refresh-token');
      expect(result).toEqual({ success: true });
      expect(RefreshToken.destroy).toHaveBeenCalledWith({ where: { token: 'some-refresh-token' } });
    });
  });

  describe('getProfile', () => {
    it('должен возвращать пользователя по ID', async () => {
      const mockUser = createMockUser();
      User.findByPk.mockResolvedValue(mockUser);

      const user = await authService.getProfile(1);
      expect(user).toEqual(mockUser);
      expect(User.findByPk).toHaveBeenCalledWith(1, expect.objectContaining({
        attributes: expect.objectContaining({ exclude: ['password_hash'] })
      }));
    });

    it('должен бросать USER_NOT_FOUND если пользователь не существует', async () => {
      User.findByPk.mockResolvedValue(null);
      await expect(authService.getProfile(999)).rejects.toThrow('USER_NOT_FOUND');
    });
  });
});
```

**Step 2: Запустить тесты**
```bash
cd backend && npx jest tests/unit/services/authService.test.js --verbose
```
Ожидаемо: все тесты PASS.

**Step 3: Commit**
```bash
git add backend/tests/unit/services/authService.test.js
git commit -m "test: add unit tests for AuthService"
```

---

### Task 4: Unit-тесты для RabbitService

**Files:**
- Create: `backend/tests/unit/services/rabbitService.test.js`

**Step 1: Написать тесты**

```js
// backend/tests/unit/services/rabbitService.test.js
jest.mock('../../../src/models', () => {
  const mockSequelize = { transaction: jest.fn() };
  return {
    Rabbit: { findOne: jest.fn(), findByPk: jest.fn(), create: jest.fn(), count: jest.fn(), findAndCountAll: jest.fn() },
    Breed: { findByPk: jest.fn() },
    Cage: { findOne: jest.fn() },
    RabbitWeight: { create: jest.fn() },
    Photo: {},
    Breeding: {},
    Birth: {},
    Vaccination: {},
    MedicalRecord: {},
    FeedingRecord: {},
    Transaction: {},
    Task: {},
    sequelize: mockSequelize
  };
});
jest.mock('../../../src/utils/fileHelper', () => ({ deleteFile: jest.fn() }));

const { Rabbit, Breed, Cage, RabbitWeight, sequelize } = require('../../../src/models');
const rabbitService = require('../../../src/services/rabbitService');
const { createMockRabbit } = require('../../helpers/mockModels');

describe('RabbitService', () => {
  beforeEach(() => jest.clearAllMocks());

  describe('createRabbit', () => {
    const mockTransaction = { commit: jest.fn(), rollback: jest.fn() };

    beforeEach(() => {
      sequelize.transaction.mockResolvedValue(mockTransaction);
    });

    it('должен бросать BREED_NOT_FOUND если порода не существует', async () => {
      Breed.findByPk.mockResolvedValue(null);

      await expect(rabbitService.createRabbit({ breed_id: 999, user_id: 1 }))
        .rejects.toThrow('BREED_NOT_FOUND');
      expect(mockTransaction.rollback).toHaveBeenCalled();
    });

    it('должен бросать CAGE_NOT_FOUND если клетка не найдена у пользователя', async () => {
      Breed.findByPk.mockResolvedValue({ id: 1 });
      Cage.findOne.mockResolvedValue(null);

      await expect(rabbitService.createRabbit({ breed_id: 1, cage_id: 1, user_id: 1 }))
        .rejects.toThrow('CAGE_NOT_FOUND');
    });

    it('должен бросать CAGE_FULL если клетка заполнена', async () => {
      Breed.findByPk.mockResolvedValue({ id: 1 });
      Cage.findOne.mockResolvedValue({ id: 1, capacity: 2 });
      Rabbit.count.mockResolvedValue(2);

      await expect(rabbitService.createRabbit({ breed_id: 1, cage_id: 1, user_id: 1, status: 'healthy' }))
        .rejects.toThrow('CAGE_FULL');
    });

    it('должен создавать кролика успешно если все данные корректны', async () => {
      const mockRabbit = { ...createMockRabbit(), toJSON: () => createMockRabbit() };
      Breed.findByPk.mockResolvedValue({ id: 1 });
      Rabbit.create.mockResolvedValue(mockRabbit);
      RabbitWeight.create.mockResolvedValue({});

      const result = await rabbitService.createRabbit({
        name: 'Буся', breed_id: 1, user_id: 1, sex: 'female',
        birth_date: '2024-01-01', status: 'healthy', weight: 2.5
      });

      expect(result).toBeDefined();
      expect(Rabbit.create).toHaveBeenCalled();
      expect(mockTransaction.commit).toHaveBeenCalled();
    });
  });
});
```

**Step 2: Запустить тесты**
```bash
cd backend && npx jest tests/unit/services/rabbitService.test.js --verbose
```
Ожидаемо: все тесты PASS.

**Step 3: Commit**
```bash
git add backend/tests/unit/services/rabbitService.test.js
git commit -m "test: add unit tests for RabbitService"
```

---

### Task 5: Настройка интеграционных тестов

**Files:**
- Create: `backend/tests/integration/helpers/testApp.js`
- Create: `backend/tests/integration/helpers/testDb.js`

**Контекст:** Интеграционные тесты запускаются против реальной тестовой БД. Нужна отдельная MySQL БД `rabbitfarm_test`.

**Step 1: Создать хелпер приложения для тестов**

```js
// backend/tests/integration/helpers/testApp.js
process.env.NODE_ENV = 'test';
process.env.DB_NAME = process.env.DB_TEST_NAME || 'rabbitfarm_test';

const app = require('../../../src/app');
module.exports = app;
```

**Step 2: Создать хелпер для работы с тестовой БД**

```js
// backend/tests/integration/helpers/testDb.js
const { sequelize } = require('../../../src/models');

const syncTestDb = async () => {
  await sequelize.sync({ force: true }); // Пересоздаёт таблицы
};

const closeTestDb = async () => {
  await sequelize.close();
};

module.exports = { syncTestDb, closeTestDb };
```

**Step 3: Добавить тестовую конфигурацию в config/database.js**

Открыть `backend/src/config/database.js` и убедиться, что `test` окружение использует значения из env:
```js
// В блоке test:
test: {
  host: process.env.DB_HOST || 'localhost',
  port: process.env.DB_PORT || 3306,
  database: process.env.DB_TEST_NAME || 'rabbitfarm_test',
  username: process.env.DB_USER || 'root',
  password: process.env.DB_PASSWORD || '',
  dialect: 'mysql',
  logging: false
}
```

**Step 4: Добавить в `.env.example`**
```
DB_TEST_NAME=rabbitfarm_test
```

**Step 5: Commit**
```bash
git add backend/tests/integration/
git commit -m "test: add integration test infrastructure"
```

---

### Task 6: Интеграционные тесты — Auth API

**Files:**
- Create: `backend/tests/integration/auth.test.js`

**Step 1: Написать интеграционные тесты для Auth**

```js
// backend/tests/integration/auth.test.js
const request = require('supertest');
const app = require('./helpers/testApp');
const { syncTestDb, closeTestDb } = require('./helpers/testDb');

describe('Auth API', () => {
  beforeAll(async () => {
    await syncTestDb();
  });

  afterAll(async () => {
    await closeTestDb();
  });

  describe('POST /api/v1/auth/register', () => {
    it('должен регистрировать нового пользователя', async () => {
      const res = await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'new@example.com', password: 'Password123!', full_name: 'Test User' });

      expect(res.status).toBe(201);
      expect(res.body.success).toBe(true);
      expect(res.body.data).toHaveProperty('access_token');
      expect(res.body.data).toHaveProperty('refresh_token');
      expect(res.body.data.user).toHaveProperty('id');
      expect(res.body.data.user).not.toHaveProperty('password_hash');
    });

    it('должен отклонять дублирующийся email', async () => {
      await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'dup@example.com', password: 'Password123!', full_name: 'User' });

      const res = await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'dup@example.com', password: 'Password123!', full_name: 'User 2' });

      expect(res.status).toBe(400);
      expect(res.body.success).toBe(false);
    });

    it('должен отклонять невалидный email', async () => {
      const res = await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'not-an-email', password: 'Password123!', full_name: 'User' });

      expect(res.status).toBe(422);
    });

    it('должен отклонять слабый пароль', async () => {
      const res = await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'weak@example.com', password: '123', full_name: 'User' });

      expect(res.status).toBe(422);
    });
  });

  describe('POST /api/v1/auth/login', () => {
    beforeAll(async () => {
      await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'login@example.com', password: 'Password123!', full_name: 'Login User' });
    });

    it('должен входить с верными данными', async () => {
      const res = await request(app)
        .post('/api/v1/auth/login')
        .send({ email: 'login@example.com', password: 'Password123!' });

      expect(res.status).toBe(200);
      expect(res.body.data).toHaveProperty('access_token');
    });

    it('должен отклонять неверный пароль', async () => {
      const res = await request(app)
        .post('/api/v1/auth/login')
        .send({ email: 'login@example.com', password: 'WrongPass!' });

      expect(res.status).toBe(401);
    });

    it('должен отклонять несуществующего пользователя', async () => {
      const res = await request(app)
        .post('/api/v1/auth/login')
        .send({ email: 'ghost@example.com', password: 'Password123!' });

      expect(res.status).toBe(401);
    });
  });

  describe('GET /api/v1/auth/me', () => {
    let accessToken;

    beforeAll(async () => {
      const res = await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'me@example.com', password: 'Password123!', full_name: 'Me User' });
      accessToken = res.body.data.access_token;
    });

    it('должен возвращать профиль авторизованного пользователя', async () => {
      const res = await request(app)
        .get('/api/v1/auth/me')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      expect(res.body.data.email).toBe('me@example.com');
    });

    it('должен отклонять запрос без токена', async () => {
      const res = await request(app).get('/api/v1/auth/me');
      expect(res.status).toBe(401);
    });

    it('должен отклонять запрос с невалидным токеном', async () => {
      const res = await request(app)
        .get('/api/v1/auth/me')
        .set('Authorization', 'Bearer invalid.token.here');
      expect(res.status).toBe(401);
    });
  });

  describe('POST /api/v1/auth/refresh', () => {
    let refreshToken;

    beforeAll(async () => {
      const res = await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'refresh@example.com', password: 'Password123!', full_name: 'Refresh User' });
      refreshToken = res.body.data.refresh_token;
    });

    it('должен обновлять access token по refresh token', async () => {
      const res = await request(app)
        .post('/api/v1/auth/refresh')
        .send({ refresh_token: refreshToken });

      expect(res.status).toBe(200);
      expect(res.body.data).toHaveProperty('access_token');
      expect(res.body.data).toHaveProperty('refresh_token');
    });

    it('должен отклонять невалидный refresh token', async () => {
      const res = await request(app)
        .post('/api/v1/auth/refresh')
        .send({ refresh_token: 'invalid-token' });

      expect(res.status).toBe(401);
    });
  });

  describe('POST /api/v1/auth/logout', () => {
    it('должен инвалидировать refresh token', async () => {
      const registerRes = await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'logout@example.com', password: 'Password123!', full_name: 'Logout User' });
      const { refresh_token } = registerRes.body.data;

      const logoutRes = await request(app)
        .post('/api/v1/auth/logout')
        .send({ refresh_token });

      expect(logoutRes.status).toBe(200);

      // После logout refresh token должен быть недействителен
      const refreshRes = await request(app)
        .post('/api/v1/auth/refresh')
        .send({ refresh_token });

      expect(refreshRes.status).toBe(401);
    });
  });
});
```

**Step 2: Запустить тесты (требует тестовую БД)**
```bash
cd backend && DB_TEST_NAME=rabbitfarm_test npx jest tests/integration/auth.test.js --verbose
```
Ожидаемо: все тесты PASS.

**Step 3: Commit**
```bash
git add backend/tests/integration/auth.test.js
git commit -m "test: add integration tests for Auth API"
```

---

### Task 7: Интеграционные тесты — Rabbits API

**Files:**
- Create: `backend/tests/integration/rabbits.test.js`

**Step 1: Написать тесты для Rabbits API**

```js
// backend/tests/integration/rabbits.test.js
const request = require('supertest');
const app = require('./helpers/testApp');
const { syncTestDb, closeTestDb } = require('./helpers/testDb');

describe('Rabbits API', () => {
  let accessToken, breedId;

  beforeAll(async () => {
    await syncTestDb();

    // Регистрируем пользователя
    const res = await request(app)
      .post('/api/v1/auth/register')
      .send({ email: 'farmer@example.com', password: 'Password123!', full_name: 'Farmer' });
    accessToken = res.body.data.access_token;

    // Создаём породу
    const breedRes = await request(app)
      .post('/api/v1/breeds')
      .set('Authorization', `Bearer ${accessToken}`)
      .send({ name: 'Серый великан', description: 'Мясная порода', average_weight: 5.0 });
    breedId = breedRes.body.data.id;
  });

  afterAll(async () => {
    await closeTestDb();
  });

  describe('POST /api/v1/rabbits', () => {
    it('должен создавать кролика', async () => {
      const res = await request(app)
        .post('/api/v1/rabbits')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({
          name: 'Буся',
          breed_id: breedId,
          sex: 'female',
          birth_date: '2024-01-01',
          status: 'healthy',
          purpose: 'breeding',
          weight: 2.5
        });

      expect(res.status).toBe(201);
      expect(res.body.data.name).toBe('Буся');
      expect(res.body.data).not.toHaveProperty('user_id');  // не утекает user_id? или утекает?
    });

    it('должен отклонять создание кролика без авторизации', async () => {
      const res = await request(app)
        .post('/api/v1/rabbits')
        .send({ name: 'Тест', breed_id: breedId, sex: 'male' });

      expect(res.status).toBe(401);
    });

    it('должен отклонять несуществующую породу', async () => {
      const res = await request(app)
        .post('/api/v1/rabbits')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ name: 'Тест', breed_id: 9999, sex: 'male', birth_date: '2024-01-01', status: 'healthy' });

      expect(res.status).toBe(404);
    });
  });

  describe('GET /api/v1/rabbits', () => {
    it('должен возвращать список кроликов пользователя', async () => {
      const res = await request(app)
        .get('/api/v1/rabbits')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      expect(res.body.data).toHaveProperty('items');
      expect(res.body.data).toHaveProperty('pagination');
      expect(Array.isArray(res.body.data.items)).toBe(true);
    });

    it('должен применять фильтр по полу', async () => {
      const res = await request(app)
        .get('/api/v1/rabbits?sex=female')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      res.body.data.items.forEach(rabbit => {
        expect(rabbit.sex).toBe('female');
      });
    });

    it('должен поддерживать пагинацию', async () => {
      const res = await request(app)
        .get('/api/v1/rabbits?page=1&limit=5')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      expect(res.body.data.pagination.page).toBe(1);
      expect(res.body.data.pagination.limit).toBe(5);
    });
  });

  describe('GET /api/v1/rabbits/:id', () => {
    let rabbitId;

    beforeAll(async () => {
      const res = await request(app)
        .post('/api/v1/rabbits')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ name: 'Гоша', breed_id: breedId, sex: 'male', birth_date: '2024-01-01', status: 'healthy' });
      rabbitId = res.body.data.id;
    });

    it('должен возвращать кролика по ID', async () => {
      const res = await request(app)
        .get(`/api/v1/rabbits/${rabbitId}`)
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      expect(res.body.data.name).toBe('Гоша');
    });

    it('должен возвращать 404 для несуществующего кролика', async () => {
      const res = await request(app)
        .get('/api/v1/rabbits/99999')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(404);
    });

    it('не должен возвращать кролика другого пользователя (IDOR защита)', async () => {
      // Создаём второго пользователя
      const res2 = await request(app)
        .post('/api/v1/auth/register')
        .send({ email: 'other@example.com', password: 'Password123!', full_name: 'Other' });
      const otherToken = res2.body.data.access_token;

      // Пытаемся получить кролика первого пользователя через второго
      const rabbitRes = await request(app)
        .get(`/api/v1/rabbits/${rabbitId}`)
        .set('Authorization', `Bearer ${otherToken}`);

      expect(rabbitRes.status).toBe(404);
    });
  });
});
```

**Step 2: Запустить тесты**
```bash
cd backend && DB_TEST_NAME=rabbitfarm_test npx jest tests/integration/rabbits.test.js --verbose
```
Ожидаемо: все тесты PASS.

**Step 3: Commit**
```bash
git add backend/tests/integration/rabbits.test.js
git commit -m "test: add integration tests for Rabbits API"
```

---

### Task 8: Интеграционные тесты — Cages и Breeding

**Files:**
- Create: `backend/tests/integration/cages.test.js`
- Create: `backend/tests/integration/breeding.test.js`

**Step 1: Тесты для Cages**

```js
// backend/tests/integration/cages.test.js
const request = require('supertest');
const app = require('./helpers/testApp');
const { syncTestDb, closeTestDb } = require('./helpers/testDb');

describe('Cages API', () => {
  let accessToken;

  beforeAll(async () => {
    await syncTestDb();
    const res = await request(app)
      .post('/api/v1/auth/register')
      .send({ email: 'cagetest@example.com', password: 'Password123!', full_name: 'Cage Tester' });
    accessToken = res.body.data.access_token;
  });

  afterAll(async () => { await closeTestDb(); });

  describe('POST /api/v1/cages', () => {
    it('должен создавать клетку', async () => {
      const res = await request(app)
        .post('/api/v1/cages')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ name: 'Клетка А1', capacity: 4, type: 'growing', location: 'Сарай 1' });

      expect(res.status).toBe(201);
      expect(res.body.data.name).toBe('Клетка А1');
      expect(res.body.data.status).toBe('available');
    });

    it('должен отклонять клетку с нулевой вместимостью', async () => {
      const res = await request(app)
        .post('/api/v1/cages')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ name: 'Плохая', capacity: 0, type: 'growing' });

      expect(res.status).toBe(422);
    });
  });

  describe('GET /api/v1/cages', () => {
    it('должен возвращать список клеток', async () => {
      const res = await request(app)
        .get('/api/v1/cages')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      expect(Array.isArray(res.body.data.items)).toBe(true);
    });
  });

  describe('DELETE /api/v1/cages/:id', () => {
    it('должен удалять пустую клетку', async () => {
      const createRes = await request(app)
        .post('/api/v1/cages')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ name: 'Для удаления', capacity: 2, type: 'growing' });
      const cageId = createRes.body.data.id;

      const delRes = await request(app)
        .delete(`/api/v1/cages/${cageId}`)
        .set('Authorization', `Bearer ${accessToken}`);

      expect(delRes.status).toBe(200);
    });
  });
});
```

**Step 2: Тесты для Breeding**

```js
// backend/tests/integration/breeding.test.js
const request = require('supertest');
const app = require('./helpers/testApp');
const { syncTestDb, closeTestDb } = require('./helpers/testDb');

describe('Breeding API', () => {
  let accessToken, maleId, femaleId;

  beforeAll(async () => {
    await syncTestDb();
    const res = await request(app)
      .post('/api/v1/auth/register')
      .send({ email: 'breeder@example.com', password: 'Password123!', full_name: 'Breeder' });
    accessToken = res.body.data.access_token;

    // Создаём породу
    const breedRes = await request(app)
      .post('/api/v1/breeds')
      .set('Authorization', `Bearer ${accessToken}`)
      .send({ name: 'Флот', description: 'Тест', average_weight: 4.0 });
    const breedId = breedRes.body.data.id;

    // Создаём самца и самку
    const maleRes = await request(app)
      .post('/api/v1/rabbits')
      .set('Authorization', `Bearer ${accessToken}`)
      .send({ name: 'Самец', breed_id: breedId, sex: 'male', birth_date: '2023-01-01', status: 'healthy' });
    maleId = maleRes.body.data.id;

    const femaleRes = await request(app)
      .post('/api/v1/rabbits')
      .set('Authorization', `Bearer ${accessToken}`)
      .send({ name: 'Самка', breed_id: breedId, sex: 'female', birth_date: '2023-01-01', status: 'healthy' });
    femaleId = femaleRes.body.data.id;
  });

  afterAll(async () => { await closeTestDb(); });

  describe('POST /api/v1/breeding', () => {
    it('должен создавать запись о вязке', async () => {
      const res = await request(app)
        .post('/api/v1/breeding')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({
          male_id: maleId,
          female_id: femaleId,
          breeding_date: '2024-06-01',
          notes: 'Первая вязка'
        });

      expect(res.status).toBe(201);
      expect(res.body.data.status).toBe('planned');
    });

    it('должен отклонять вязку самца с самцом', async () => {
      const res = await request(app)
        .post('/api/v1/breeding')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ male_id: maleId, female_id: maleId, breeding_date: '2024-06-01' });

      expect(res.status).toBeGreaterThanOrEqual(400);
    });
  });

  describe('GET /api/v1/breeding', () => {
    it('должен возвращать список вязок', async () => {
      const res = await request(app)
        .get('/api/v1/breeding')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      expect(Array.isArray(res.body.data.items)).toBe(true);
    });
  });
});
```

**Step 3: Запустить тесты**
```bash
cd backend && DB_TEST_NAME=rabbitfarm_test npx jest tests/integration/cages.test.js tests/integration/breeding.test.js --verbose
```

**Step 4: Commit**
```bash
git add backend/tests/integration/
git commit -m "test: add integration tests for Cages and Breeding APIs"
```

---

### Task 9: Интеграционные тесты — Health, Finance, Tasks

**Files:**
- Create: `backend/tests/integration/health.test.js`
- Create: `backend/tests/integration/finance.test.js`
- Create: `backend/tests/integration/tasks.test.js`

**Step 1: Тесты Health (Vaccinations)**

```js
// backend/tests/integration/health.test.js
const request = require('supertest');
const app = require('./helpers/testApp');
const { syncTestDb, closeTestDb } = require('./helpers/testDb');

describe('Health API (Vaccinations & Medical Records)', () => {
  let accessToken, rabbitId;

  beforeAll(async () => {
    await syncTestDb();
    const authRes = await request(app)
      .post('/api/v1/auth/register')
      .send({ email: 'vet@example.com', password: 'Password123!', full_name: 'Vet' });
    accessToken = authRes.body.data.access_token;

    const breedRes = await request(app)
      .post('/api/v1/breeds')
      .set('Authorization', `Bearer ${accessToken}`)
      .send({ name: 'Тест', description: 'Тест', average_weight: 3.0 });

    const rabbitRes = await request(app)
      .post('/api/v1/rabbits')
      .set('Authorization', `Bearer ${accessToken}`)
      .send({ name: 'Пациент', breed_id: breedRes.body.data.id, sex: 'male', birth_date: '2024-01-01', status: 'healthy' });
    rabbitId = rabbitRes.body.data.id;
  });

  afterAll(async () => { await closeTestDb(); });

  describe('POST /api/v1/rabbits/:id/vaccinations', () => {
    it('должен создавать запись о вакцинации', async () => {
      const res = await request(app)
        .post(`/api/v1/rabbits/${rabbitId}/vaccinations`)
        .set('Authorization', `Bearer ${accessToken}`)
        .send({
          vaccine_name: 'ВГБК',
          vaccine_type: 'VHSV',
          vaccination_date: '2024-06-01',
          next_vaccination_date: '2024-12-01',
          dosage: '0.5 мл',
          administered_by: 'Ветеринар Иванов'
        });

      expect(res.status).toBe(201);
      expect(res.body.data.vaccine_name).toBe('ВГБК');
    });
  });

  describe('GET /api/v1/rabbits/:id/vaccinations', () => {
    it('должен возвращать историю вакцинаций', async () => {
      const res = await request(app)
        .get(`/api/v1/rabbits/${rabbitId}/vaccinations`)
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      expect(Array.isArray(res.body.data.items)).toBe(true);
    });
  });
});
```

**Step 2: Тесты Finance**

```js
// backend/tests/integration/finance.test.js
const request = require('supertest');
const app = require('./helpers/testApp');
const { syncTestDb, closeTestDb } = require('./helpers/testDb');

describe('Finance API', () => {
  let accessToken;

  beforeAll(async () => {
    await syncTestDb();
    const res = await request(app)
      .post('/api/v1/auth/register')
      .send({ email: 'accountant@example.com', password: 'Password123!', full_name: 'Accountant' });
    accessToken = res.body.data.access_token;
  });

  afterAll(async () => { await closeTestDb(); });

  describe('POST /api/v1/transactions', () => {
    it('должен создавать транзакцию дохода', async () => {
      const res = await request(app)
        .post('/api/v1/transactions')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({
          type: 'income',
          amount: 5000,
          category: 'sale',
          description: 'Продажа кролика',
          transaction_date: '2024-06-01'
        });

      expect(res.status).toBe(201);
      expect(res.body.data.type).toBe('income');
      expect(parseFloat(res.body.data.amount)).toBe(5000);
    });

    it('должен создавать транзакцию расхода', async () => {
      const res = await request(app)
        .post('/api/v1/transactions')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({
          type: 'expense',
          amount: 1000,
          category: 'feed',
          description: 'Корм',
          transaction_date: '2024-06-01'
        });

      expect(res.status).toBe(201);
      expect(res.body.data.type).toBe('expense');
    });

    it('должен отклонять транзакцию с нулевой суммой', async () => {
      const res = await request(app)
        .post('/api/v1/transactions')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ type: 'income', amount: 0, category: 'sale', transaction_date: '2024-06-01' });

      expect(res.status).toBe(422);
    });
  });

  describe('GET /api/v1/transactions', () => {
    it('должен возвращать транзакции пользователя с пагинацией', async () => {
      const res = await request(app)
        .get('/api/v1/transactions')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
      expect(res.body.data).toHaveProperty('items');
      expect(res.body.data).toHaveProperty('pagination');
    });
  });
});
```

**Step 3: Тесты Tasks**

```js
// backend/tests/integration/tasks.test.js
const request = require('supertest');
const app = require('./helpers/testApp');
const { syncTestDb, closeTestDb } = require('./helpers/testDb');

describe('Tasks API', () => {
  let accessToken;

  beforeAll(async () => {
    await syncTestDb();
    const res = await request(app)
      .post('/api/v1/auth/register')
      .send({ email: 'manager@example.com', password: 'Password123!', full_name: 'Manager' });
    accessToken = res.body.data.access_token;
  });

  afterAll(async () => { await closeTestDb(); });

  describe('POST /api/v1/tasks', () => {
    it('должен создавать задачу', async () => {
      const res = await request(app)
        .post('/api/v1/tasks')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({
          title: 'Покормить кроликов',
          description: 'Ежедневное кормление',
          priority: 'high',
          due_date: '2024-12-31',
          status: 'pending'
        });

      expect(res.status).toBe(201);
      expect(res.body.data.title).toBe('Покормить кроликов');
      expect(res.body.data.priority).toBe('high');
    });
  });

  describe('GET /api/v1/tasks', () => {
    it('должен возвращать задачи', async () => {
      const res = await request(app)
        .get('/api/v1/tasks')
        .set('Authorization', `Bearer ${accessToken}`);

      expect(res.status).toBe(200);
    });
  });

  describe('PUT /api/v1/tasks/:id', () => {
    it('должен обновлять статус задачи', async () => {
      const createRes = await request(app)
        .post('/api/v1/tasks')
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ title: 'Задача', priority: 'low', due_date: '2024-12-31', status: 'pending' });

      const taskId = createRes.body.data.id;

      const updateRes = await request(app)
        .put(`/api/v1/tasks/${taskId}`)
        .set('Authorization', `Bearer ${accessToken}`)
        .send({ status: 'completed' });

      expect(updateRes.status).toBe(200);
      expect(updateRes.body.data.status).toBe('completed');
    });
  });
});
```

**Step 4: Запустить все интеграционные тесты**
```bash
cd backend && DB_TEST_NAME=rabbitfarm_test npx jest tests/integration/ --verbose
```

**Step 5: Commit**
```bash
git add backend/tests/integration/
git commit -m "test: add integration tests for Health, Finance, and Tasks APIs"
```

---

## ФАЗА 2: БЕЗОПАСНОСТЬ

### Task 10: Blacklist для access token при logout

**Проблема:** При logout удаляется refresh token, но access token остаётся валидным до истечения (15 минут).

**Files:**
- Create: `backend/src/models/TokenBlacklist.js`
- Modify: `backend/src/models/index.js`
- Modify: `backend/src/services/authService.js`
- Modify: `backend/src/middleware/auth.js`

**Step 1: Написать тест (TDD)**

```js
// Добавить в backend/tests/integration/auth.test.js
describe('Blacklist: logout invalidates access token', () => {
  it('access token не должен работать после logout', async () => {
    const res = await request(app)
      .post('/api/v1/auth/register')
      .send({ email: 'blacklist@example.com', password: 'Password123!', full_name: 'BL User' });
    const { access_token, refresh_token } = res.body.data;

    // Logout
    await request(app)
      .post('/api/v1/auth/logout')
      .send({ refresh_token });

    // Попытка использовать access token после logout
    const meRes = await request(app)
      .get('/api/v1/auth/me')
      .set('Authorization', `Bearer ${access_token}`);

    expect(meRes.status).toBe(401);
  });
});
```

**Step 2: Создать модель TokenBlacklist**

```js
// backend/src/models/TokenBlacklist.js
const { DataTypes } = require('sequelize');

module.exports = (sequelize) => {
  const TokenBlacklist = sequelize.define('TokenBlacklist', {
    id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true },
    token_jti: { type: DataTypes.STRING(500), allowNull: false, unique: true },
    expires_at: { type: DataTypes.DATE, allowNull: false }
  }, {
    tableName: 'token_blacklist',
    timestamps: true,
    updatedAt: false,
    indexes: [
      { fields: ['token_jti'] },
      { fields: ['expires_at'] }
    ]
  });

  return TokenBlacklist;
};
```

**Step 3: Добавить jti в JWT и внести TokenBlacklist в models/index.js**

В `backend/src/utils/jwt.js` добавить `jti` (JWT ID) в payload:
```js
const { randomUUID } = require('crypto');

static generateAccessToken(payload) {
  return jwt.sign(
    { ...payload, jti: randomUUID() },
    jwtConfig.secret,
    { expiresIn: jwtConfig.expiresIn, algorithm: jwtConfig.algorithm }
  );
}
```

В `backend/src/models/index.js` зарегистрировать TokenBlacklist.

**Step 4: Обновить authService.logout — добавить jti в blacklist**

```js
async logout(refreshToken, accessToken) {
  const transaction = await User.sequelize.transaction();
  try {
    // Удаляем refresh token
    await RefreshToken.destroy({ where: { token: refreshToken }, transaction });

    // Добавляем access token в blacklist
    if (accessToken) {
      const decoded = JWTUtil.decode(accessToken);
      if (decoded && decoded.jti && decoded.exp) {
        await TokenBlacklist.create({
          token_jti: decoded.jti,
          expires_at: new Date(decoded.exp * 1000)
        }, { transaction });
      }
    }

    await transaction.commit();
    return { success: true };
  } catch (error) {
    await transaction.rollback();
    throw error;
  }
}
```

**Step 5: Обновить middleware auth.js — проверять blacklist**

```js
const authenticate = async (req, res, next) => {
  // ... существующая проверка токена ...
  const decoded = JWTUtil.verifyAccessToken(token);

  // Проверяем blacklist
  if (decoded.jti) {
    const blacklisted = await TokenBlacklist.findOne({ where: { token_jti: decoded.jti } });
    if (blacklisted) {
      return ApiResponse.unauthorized(res, 'Token has been revoked');
    }
  }
  // ... остальная логика ...
};
```

**Step 6: Обновить authController.logout — передавать access token**

```js
async logout(req, res, next) {
  const { refresh_token } = req.body;
  const authHeader = req.headers.authorization;
  const accessToken = authHeader?.startsWith('Bearer ') ? authHeader.substring(7) : null;
  await authService.logout(refresh_token, accessToken);
  // ...
}
```

**Step 7: Запустить тест**
```bash
cd backend && DB_TEST_NAME=rabbitfarm_test npx jest tests/integration/auth.test.js -t "Blacklist" --verbose
```
Ожидаемо: PASS.

**Step 8: Commit**
```bash
git add backend/src/models/TokenBlacklist.js backend/src/models/index.js backend/src/services/authService.js backend/src/middleware/auth.js backend/src/controllers/authController.js backend/src/utils/jwt.js
git commit -m "feat(security): add access token blacklist on logout"
```

---

### Task 11: Очистка expired токенов (cron job)

**Files:**
- Create: `backend/src/jobs/tokenCleanup.js`
- Modify: `backend/src/server.js`

**Step 1: Написать тест**
```js
// backend/tests/unit/jobs/tokenCleanup.test.js
jest.mock('../../../src/models', () => ({
  RefreshToken: { destroy: jest.fn().mockResolvedValue(5) },
  TokenBlacklist: { destroy: jest.fn().mockResolvedValue(3) }
}));
const { cleanExpiredTokens } = require('../../../src/jobs/tokenCleanup');
const { RefreshToken, TokenBlacklist } = require('../../../src/models');

describe('tokenCleanup', () => {
  it('должен удалять просроченные refresh и blacklist токены', async () => {
    const result = await cleanExpiredTokens();
    expect(RefreshToken.destroy).toHaveBeenCalled();
    expect(TokenBlacklist.destroy).toHaveBeenCalled();
    expect(result.refreshTokens).toBe(5);
    expect(result.blacklistTokens).toBe(3);
  });
});
```

**Step 2: Создать job**

```js
// backend/src/jobs/tokenCleanup.js
const { Op } = require('sequelize');
const { RefreshToken, TokenBlacklist } = require('../models');
const logger = require('../utils/logger');

const cleanExpiredTokens = async () => {
  const now = new Date();
  const [refreshTokens, blacklistTokens] = await Promise.all([
    RefreshToken.destroy({ where: { expires_at: { [Op.lt]: now } } }),
    TokenBlacklist.destroy({ where: { expires_at: { [Op.lt]: now } } })
  ]);
  logger.info(`Token cleanup: removed ${refreshTokens} refresh tokens, ${blacklistTokens} blacklist entries`);
  return { refreshTokens, blacklistTokens };
};

// Запускать каждый час
const startTokenCleanupJob = () => {
  const INTERVAL = 60 * 60 * 1000; // 1 час
  setInterval(async () => {
    try {
      await cleanExpiredTokens();
    } catch (error) {
      logger.error('Token cleanup job failed', { error: error.message });
    }
  }, INTERVAL);
  logger.info('Token cleanup job started (interval: 1h)');
};

module.exports = { cleanExpiredTokens, startTokenCleanupJob };
```

**Step 3: Подключить в server.js**

```js
// В backend/src/server.js после запуска сервера:
const { startTokenCleanupJob } = require('./jobs/tokenCleanup');
startTokenCleanupJob();
```

**Step 4: Запустить тест**
```bash
cd backend && npx jest tests/unit/jobs/ --verbose
```

**Step 5: Commit**
```bash
git add backend/src/jobs/ backend/src/server.js backend/tests/unit/jobs/
git commit -m "feat: add automatic token cleanup job (runs every hour)"
```

---

### Task 12: Валидация env-переменных при старте

**Files:**
- Create: `backend/src/config/validateEnv.js`
- Modify: `backend/src/server.js`

**Step 1: Создать валидатор конфигурации**

```js
// backend/src/config/validateEnv.js
const REQUIRED_ENV = [
  'JWT_SECRET',
  'JWT_REFRESH_SECRET',
  'DB_HOST',
  'DB_NAME',
  'DB_USER',
  'DB_PASSWORD'
];

const WEAK_SECRETS = [
  'secret', 'password', 'changeme', 'rabbitfarm_jwt_secret', 'test'
];

const validateEnv = () => {
  const missing = REQUIRED_ENV.filter(key => !process.env[key]);
  if (missing.length > 0) {
    throw new Error(`Missing required environment variables: ${missing.join(', ')}`);
  }

  if (process.env.NODE_ENV === 'production') {
    const jwtSecret = process.env.JWT_SECRET || '';
    if (jwtSecret.length < 32) {
      throw new Error('JWT_SECRET must be at least 32 characters in production');
    }
    if (WEAK_SECRETS.some(weak => jwtSecret.toLowerCase().includes(weak))) {
      throw new Error('JWT_SECRET appears to be a default/weak value. Change it in production!');
    }
  }
};

module.exports = validateEnv;
```

**Step 2: Вызвать в server.js до запуска**

```js
const validateEnv = require('./config/validateEnv');
validateEnv(); // Бросит исключение если конфигурация неправильная
```

**Step 3: Обновить .env.example с явными инструкциями**

```
# ОБЯЗАТЕЛЬНО СМЕНИТЬ В ПРОДАКШНЕ!
JWT_SECRET=<generate: openssl rand -base64 48>
JWT_REFRESH_SECRET=<generate: openssl rand -base64 48>
DB_TEST_NAME=rabbitfarm_test
```

**Step 4: Commit**
```bash
git add backend/src/config/validateEnv.js backend/src/server.js backend/.env.example
git commit -m "feat(security): add startup env validation with weak-secret detection"
```

---

## ФАЗА 3: ФУНКЦИОНАЛЬНОСТЬ

### Task 13: Password Reset Flow

**Files:**
- Create: `backend/src/models/PasswordResetToken.js`
- Modify: `backend/src/models/index.js`
- Modify: `backend/src/services/authService.js`
- Modify: `backend/src/controllers/authController.js`
- Modify: `backend/src/routes/auth.routes.js`
- Modify: `backend/src/validators/authValidator.js`

**Контекст:** Так как у проекта нет email-сервера, реализуем "admin-flow": генерируем одноразовый токен (возвращаем его в ответе или показываем в логах), который можно передать пользователю по другому каналу. В реальном приложении здесь подключается email. Endpoint: `POST /api/v1/auth/forgot-password` → возвращает reset_token (только в не-production или для тестирования), `POST /api/v1/auth/reset-password` → принимает токен + новый пароль.

**Step 1: Написать тест (TDD)**

```js
// Добавить в tests/integration/auth.test.js
describe('Password Reset', () => {
  let resetEmail = 'reset@example.com';

  beforeAll(async () => {
    await request(app)
      .post('/api/v1/auth/register')
      .send({ email: resetEmail, password: 'OldPass123!', full_name: 'Reset User' });
  });

  it('должен генерировать reset token для существующего email', async () => {
    const res = await request(app)
      .post('/api/v1/auth/forgot-password')
      .send({ email: resetEmail });

    expect(res.status).toBe(200);
    expect(res.body.data).toHaveProperty('reset_token'); // в test-режиме возвращается токен
  });

  it('должен успешно сбрасывать пароль с валидным токеном', async () => {
    const forgotRes = await request(app)
      .post('/api/v1/auth/forgot-password')
      .send({ email: resetEmail });
    const { reset_token } = forgotRes.body.data;

    const resetRes = await request(app)
      .post('/api/v1/auth/reset-password')
      .send({ token: reset_token, new_password: 'NewPass456!' });

    expect(resetRes.status).toBe(200);

    // Старый пароль не должен работать
    const oldLoginRes = await request(app)
      .post('/api/v1/auth/login')
      .send({ email: resetEmail, password: 'OldPass123!' });
    expect(oldLoginRes.status).toBe(401);

    // Новый пароль должен работать
    const newLoginRes = await request(app)
      .post('/api/v1/auth/login')
      .send({ email: resetEmail, password: 'NewPass456!' });
    expect(newLoginRes.status).toBe(200);
  });

  it('должен отклонять несуществующий reset token', async () => {
    const res = await request(app)
      .post('/api/v1/auth/reset-password')
      .send({ token: 'fake-token-12345', new_password: 'NewPass123!' });

    expect(res.status).toBe(400);
  });
});
```

**Step 2: Создать модель PasswordResetToken**

```js
// backend/src/models/PasswordResetToken.js
const { DataTypes } = require('sequelize');

module.exports = (sequelize) => {
  const PasswordResetToken = sequelize.define('PasswordResetToken', {
    id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true },
    user_id: { type: DataTypes.INTEGER, allowNull: false },
    token: { type: DataTypes.STRING(64), allowNull: false, unique: true },
    expires_at: { type: DataTypes.DATE, allowNull: false },
    used: { type: DataTypes.BOOLEAN, defaultValue: false }
  }, {
    tableName: 'password_reset_tokens',
    timestamps: true,
    updatedAt: false
  });

  return PasswordResetToken;
};
```

**Step 3: Добавить методы в authService**

```js
async forgotPassword(email) {
  const user = await User.findOne({ where: { email } });
  // Не раскрываем существование аккаунта в ответе (security)
  // Но для тестирования в test-режиме возвращаем токен
  if (!user) {
    return { message: 'If email exists, reset instructions were sent' };
  }

  const { randomBytes } = require('crypto');
  const token = randomBytes(32).toString('hex');
  const expiresAt = new Date(Date.now() + 60 * 60 * 1000); // 1 час

  // Инвалидируем предыдущие токены
  await PasswordResetToken.destroy({ where: { user_id: user.id } });

  await PasswordResetToken.create({ user_id: user.id, token, expires_at: expiresAt });

  logger.info('Password reset token created', { userId: user.id });

  // В production: отправить email. В dev/test: вернуть токен
  const result = { message: 'If email exists, reset instructions were sent' };
  if (process.env.NODE_ENV !== 'production') {
    result.reset_token = token; // Только для разработки/тестов
  }
  return result;
}

async resetPassword(token, newPassword) {
  const resetRecord = await PasswordResetToken.findOne({
    where: { token, used: false },
    include: [{ model: User }]
  });

  if (!resetRecord || new Date() > resetRecord.expires_at) {
    throw new Error('INVALID_OR_EXPIRED_RESET_TOKEN');
  }

  const transaction = await User.sequelize.transaction();
  try {
    const newPasswordHash = await PasswordUtil.hash(newPassword);
    await resetRecord.User.update({ password_hash: newPasswordHash }, { transaction });
    await resetRecord.update({ used: true }, { transaction });
    // Инвалидировать все refresh токены
    await RefreshToken.destroy({ where: { user_id: resetRecord.user_id }, transaction });
    await transaction.commit();
    return { success: true };
  } catch (error) {
    await transaction.rollback();
    throw error;
  }
}
```

**Step 4: Добавить маршруты и контроллер методы** (аналогично существующим, по образцу `changePassword`)

**Step 5: Запустить тесты**
```bash
cd backend && DB_TEST_NAME=rabbitfarm_test npx jest tests/integration/auth.test.js -t "Password Reset" --verbose
```

**Step 6: Commit**
```bash
git add backend/src/models/PasswordResetToken.js backend/src/models/index.js backend/src/services/authService.js backend/src/controllers/authController.js backend/src/routes/auth.routes.js backend/src/validators/authValidator.js
git commit -m "feat: implement password reset flow"
```

---

## ФАЗА 4: ДОКУМЕНТАЦИЯ (Swagger/OpenAPI)

### Task 14: Подключить swagger-jsdoc + swagger-ui-express

**Files:**
- Modify: `backend/package.json` (добавить зависимости)
- Create: `backend/src/config/swagger.js`
- Modify: `backend/src/app.js`

**Step 1: Установить пакеты**
```bash
cd backend && npm install swagger-jsdoc swagger-ui-express
```

**Step 2: Создать конфиг Swagger**

```js
// backend/src/config/swagger.js
const swaggerJsdoc = require('swagger-jsdoc');

const options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'RabbitFarm API',
      version: '1.0.0',
      description: 'REST API для управления кроличьей фермой',
      contact: { name: 'RabbitFarm Support' }
    },
    servers: [
      { url: '/api/v1', description: 'API v1' }
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT'
        }
      },
      schemas: {
        Error: {
          type: 'object',
          properties: {
            success: { type: 'boolean', example: false },
            error: {
              type: 'object',
              properties: {
                code: { type: 'string' },
                message: { type: 'string' }
              }
            }
          }
        },
        Pagination: {
          type: 'object',
          properties: {
            page: { type: 'integer' },
            limit: { type: 'integer' },
            total: { type: 'integer' },
            totalPages: { type: 'integer' }
          }
        }
      }
    },
    security: [{ bearerAuth: [] }]
  },
  apis: ['./src/routes/*.js']
};

module.exports = swaggerJsdoc(options);
```

**Step 3: Подключить UI в app.js**

```js
const swaggerUi = require('swagger-ui-express');
const swaggerSpec = require('./config/swagger');

// После middleware, перед routes:
app.use('/api/docs', swaggerUi.serve, swaggerUi.setup(swaggerSpec));
app.get('/api/docs.json', (req, res) => res.json(swaggerSpec));
```

**Step 4: Commit**
```bash
git add backend/src/config/swagger.js backend/src/app.js backend/package.json backend/package-lock.json
git commit -m "feat(docs): add Swagger/OpenAPI documentation endpoint"
```

---

### Task 15: JSDoc-аннотации для Auth routes

**Files:**
- Modify: `backend/src/routes/auth.routes.js`

**Step 1: Добавить JSDoc-аннотации к роутам**

```js
/**
 * @swagger
 * components:
 *   schemas:
 *     User:
 *       type: object
 *       properties:
 *         id: { type: integer, example: 1 }
 *         email: { type: string, format: email, example: "user@example.com" }
 *         full_name: { type: string, example: "Иван Иванов" }
 *         role: { type: string, enum: [owner, manager, worker] }
 *         is_active: { type: boolean }
 *     AuthResponse:
 *       type: object
 *       properties:
 *         access_token: { type: string }
 *         refresh_token: { type: string }
 *         user: { $ref: '#/components/schemas/User' }
 */

/**
 * @swagger
 * /auth/register:
 *   post:
 *     summary: Регистрация нового пользователя
 *     tags: [Auth]
 *     security: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             required: [email, password, full_name]
 *             properties:
 *               email: { type: string, format: email }
 *               password: { type: string, minLength: 6 }
 *               full_name: { type: string }
 *     responses:
 *       201:
 *         description: Пользователь создан
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/AuthResponse'
 *       400:
 *         description: Email уже занят
 *       422:
 *         description: Ошибка валидации
 */

/**
 * @swagger
 * /auth/login:
 *   post:
 *     summary: Вход в систему
 *     tags: [Auth]
 *     security: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             required: [email, password]
 *             properties:
 *               email: { type: string, format: email }
 *               password: { type: string }
 *     responses:
 *       200:
 *         description: Успешный вход
 *       401:
 *         description: Неверные данные
 */

/**
 * @swagger
 * /auth/me:
 *   get:
 *     summary: Получить текущий профиль
 *     tags: [Auth]
 *     responses:
 *       200:
 *         description: Профиль пользователя
 *         content:
 *           application/json:
 *             schema:
 *               $ref: '#/components/schemas/User'
 *       401:
 *         description: Не авторизован
 */

/**
 * @swagger
 * /auth/logout:
 *   post:
 *     summary: Выход из системы
 *     tags: [Auth]
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               refresh_token: { type: string }
 *     responses:
 *       200:
 *         description: Успешный выход
 */

/**
 * @swagger
 * /auth/forgot-password:
 *   post:
 *     summary: Запросить сброс пароля
 *     tags: [Auth]
 *     security: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             required: [email]
 *             properties:
 *               email: { type: string, format: email }
 *     responses:
 *       200:
 *         description: Инструкции отправлены
 */

/**
 * @swagger
 * /auth/reset-password:
 *   post:
 *     summary: Сбросить пароль по токену
 *     tags: [Auth]
 *     security: []
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             required: [token, new_password]
 *             properties:
 *               token: { type: string }
 *               new_password: { type: string, minLength: 6 }
 *     responses:
 *       200:
 *         description: Пароль сброшен
 *       400:
 *         description: Токен недействителен
 */
```

**Step 2: Commit**
```bash
git add backend/src/routes/auth.routes.js
git commit -m "docs: add OpenAPI annotations for Auth routes"
```

---

### Task 16: JSDoc-аннотации для Rabbits и Cages routes

**Files:**
- Modify: `backend/src/routes/rabbit.routes.js`
- Modify: `backend/src/routes/cage.routes.js`

**Step 1: Добавить аннотации в rabbit.routes.js** (по образцу Task 15)

Ключевые endpoints для документирования:
- `GET /rabbits` — список с параметрами: page, limit, sex, status, breed_id, search
- `POST /rabbits` — создание с полями: name, breed_id, sex, birth_date, status, purpose, weight, tag_id, cage_id, father_id, mother_id, notes
- `GET /rabbits/:id` — детали с pedigree и Breed
- `PUT /rabbits/:id` — обновление
- `DELETE /rabbits/:id` — удаление
- `GET /rabbits/:id/weights` — история весов
- `POST /rabbits/:id/weights` — добавление веса
- `GET /rabbits/:id/pedigree` — родословная

**Step 2: Аннотировать cage.routes.js** (аналогично)

**Step 3: Commit**
```bash
git add backend/src/routes/rabbit.routes.js backend/src/routes/cage.routes.js
git commit -m "docs: add OpenAPI annotations for Rabbits and Cages routes"
```

---

### Task 17: JSDoc-аннотации для остальных routes

**Files:**
- Modify: `backend/src/routes/breeding.routes.js`
- Modify: `backend/src/routes/vaccination.routes.js`
- Modify: `backend/src/routes/medical-record.routes.js`
- Modify: `backend/src/routes/feed.routes.js`
- Modify: `backend/src/routes/feeding-record.routes.js`
- Modify: `backend/src/routes/transaction.routes.js`
- Modify: `backend/src/routes/task.routes.js`
- Modify: `backend/src/routes/report.routes.js`

(Добавить `@swagger` аннотации к каждому роуту по образцу Task 15-16)

**Step 1: Commit после каждого файла или все вместе**
```bash
git add backend/src/routes/
git commit -m "docs: add OpenAPI annotations for all remaining routes"
```

---

## ФАЗА 5: КАЧЕСТВО КОДА И ПОДДЕРЖИВАЕМОСТЬ

### Task 18: Вынести бизнес-логику из контроллеров в сервисы

**Проблема:** Некоторые контроллеры (transaction, task, cage, feed) не имеют отдельного сервисного слоя — логика прямо в контроллере.

**Files:**
- Create: `backend/src/services/transactionService.js`
- Create: `backend/src/services/taskService.js`
- Create: `backend/src/services/cageService.js`
- Create: `backend/src/services/feedService.js`
- Modify: соответствующие контроллеры

**Step 1: Проверить какие контроллеры используют сервисы**
```bash
grep -l "require.*services" backend/src/controllers/*.js
```

**Step 2: Для каждого контроллера без сервиса — создать сервис**

Паттерн (пример для transactionService.js):
```js
const { Transaction } = require('../models');
const { Op } = require('sequelize');
const logger = require('../utils/logger');

class TransactionService {
  async createTransaction(data) { /* ... */ }
  async getTransactions(userId, filters) { /* ... */ }
  async updateTransaction(id, userId, data) { /* ... */ }
  async deleteTransaction(id, userId) { /* ... */ }
  async getFinancialSummary(userId, dateFrom, dateTo) { /* ... */ }
}

module.exports = new TransactionService();
```

**Step 3: Commit после каждого сервиса**
```bash
git commit -m "refactor: extract TransactionService from controller"
git commit -m "refactor: extract TaskService from controller"
```

---

### Task 19: Устранить дублирование логики валидации

**Проблема:** Некоторые проверки дублируются в middleware и сервисах.

**Files:**
- Modify: `backend/src/services/rabbitService.js`
- Modify: `backend/src/services/breedingService.js`

**Step 1: Аудит дублирования**

Проверить: если Joi-схема уже валидирует поле (тип, диапазон), то в сервисе не нужна повторная проверка. Оставить в сервисе только бизнес-логические проверки (CAGE_FULL, FATHER_NOT_FOUND, etc.).

**Step 2: Commit**
```bash
git commit -m "refactor: remove validation duplication between middleware and services"
```

---

### Task 20: Финальный прогон всех тестов и coverage-отчёт

**Step 1: Запустить все unit-тесты**
```bash
cd backend && npx jest tests/unit/ --coverage --verbose
```
Ожидаемо: все PASS, coverage > 80%.

**Step 2: Запустить все интеграционные тесты**
```bash
cd backend && DB_TEST_NAME=rabbitfarm_test npx jest tests/integration/ --verbose
```
Ожидаемо: все PASS.

**Step 3: Запустить весь suite**
```bash
cd backend && DB_TEST_NAME=rabbitfarm_test npx jest --coverage
```

**Step 4: Проверить покрытие**
Если coverage < 80%: добавить тесты для непокрытых случаев.

**Step 5: Commit финального состояния**
```bash
git add .
git commit -m "test: all tests passing, coverage > 80%"
```

---

## Итоговый Roadmap по оценкам

| Фаза | Tasks | Влияние |
|------|-------|---------|
| **Фаза 1: Тесты** | 1–9 | Покрытие 1→10, Поддерживаемость 8→9 |
| **Фаза 2: Безопасность** | 10–12 | Безопасность 8→10 |
| **Фаза 3: Функциональность** | 13 | Функциональность 9→10 |
| **Фаза 4: Документация** | 14–17 | Документация 7→10 |
| **Фаза 5: Код** | 18–20 | Качество 8→10, Поддерживаемость 9→10 |

**Итого: 20 tasks → все параметры 10/10**
