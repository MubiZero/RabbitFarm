# 🚀 RabbitFarm - Quick Start Guide

## Быстрый старт проекта

### Предварительные требования

**Backend:**
- Node.js 18+
- MySQL 8.0+
- npm или yarn

**Mobile:**
- Flutter 3.9.2+
- Dart 3.0+
- Android Studio / VS Code
- Android SDK или Xcode (для iOS)

---

## 🔧 Backend Setup

### 1. Установка зависимостей

```bash
cd backend
npm install
```

### 2. Настройка окружения

Создайте файл `.env` в папке `backend/`:

```env
# Server
PORT=3000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=rabbitfarm
DB_DIALECT=mysql

# JWT
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=your_refresh_secret_key
JWT_REFRESH_EXPIRES_IN=7d

# API
API_VERSION=v1
```

### 3. Создание базы данных

```bash
# Автоматическое создание БД
npm run db:create

# Или вручную в MySQL:
mysql -u root -p
CREATE DATABASE rabbitfarm CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. Запуск миграций

```bash
npm run db:migrate
```

### 5. (Опционально) Заполнение тестовыми данными

```bash
npm run db:seed
```

### 6. Запуск сервера

```bash
# Development с hot reload
npm run dev

# Production
npm start
```

**Backend доступен:** http://localhost:3000/api/v1

**Проверка работы:**
```bash
curl http://localhost:3000/api/v1/health
```

---

## 📱 Mobile Setup

### 1. Установка зависимостей

```bash
cd mobile
flutter pub get
```

### 2. Генерация кода (Freezed)

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. Настройка API endpoint

Откройте `mobile/lib/core/api/api_endpoints.dart` и настройте базовый URL:

```dart
class ApiEndpoints {
  // Для эмулятора Android
  static const String baseUrl = 'http://10.0.2.2:3000/api/v1';

  // Для iOS симулятора
  // static const String baseUrl = 'http://localhost:3000/api/v1';

  // Для физического устройства (замените на IP вашего ПК)
  // static const String baseUrl = 'http://192.168.1.XXX:3000/api/v1';

  // ...
}
```

**Как узнать IP вашего ПК:**

Windows:
```bash
ipconfig
# Найдите IPv4 Address в секции вашего адаптера
```

macOS/Linux:
```bash
ifconfig
# Или
ip addr show
```

### 4. Запуск приложения

```bash
# Проверить подключенные устройства
flutter devices

# Запустить на конкретном устройстве
flutter run -d <device_id>

# Или просто
flutter run
```

---

## 🧪 Тестирование

### Создание тестового пользователя

**Через API:**
```bash
curl -X POST http://localhost:3000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "email": "admin@rabbitfarm.com",
    "password": "admin123",
    "full_name": "Администратор"
  }'
```

**Через Mobile App:**
1. Запустите приложение
2. Нажмите "Регистрация"
3. Заполните форму
4. Войдите с созданными учетными данными

---

## 📚 Доступные API эндпоинты

### Основные модули:

| Модуль | Endpoint | Описание |
|--------|----------|----------|
| Auth | `/auth/login` | Вход в систему |
| Auth | `/auth/register` | Регистрация |
| Rabbits | `/rabbits` | Список кроликов |
| Breeds | `/breeds` | Породы |
| Cages | `/cages` | Клетки |
| Breeding | `/breeding` | Случки |
| Births | `/births` | Рождения |
| Vaccinations | `/vaccinations` | Вакцинации |
| Medical Records | `/medical-records` | Медкарты |
| Feeds | `/feeds` | Корма |
| Feeding Records | `/feeding-records` | Кормление |
| Transactions | `/transactions` | Финансы |
| Tasks | `/tasks` | Задачи |
| Reports | `/reports/dashboard` | Dashboard |

**Полная документация:** см. [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)

---

## 🎯 Доступные экраны в Mobile

После входа доступны:

1. **Dashboard** (`/dashboard`) - Главная сводка фермы
2. **Кролики** (`/rabbits`) - Управление кроликами
3. **Породы** (`/breeds`) - Управление породами
4. **Клетки** (`/cages`) - Управление клетками
5. **Разведение** (`/breeding/planner`) - Планирование случек
6. **Рождения** (`/births`) - Регистрация рождений
7. **Вакцинации** (`/vaccinations`) - Прививки
8. **Медкарты** (`/medical-records`) - Медицинские записи
9. **Корма** (`/feeds`) - Управление кормами
10. **Кормление** (`/feeding-records`) - Записи кормления
11. **Финансы** (`/transactions`) - Доходы и расходы
12. **Задачи** (`/tasks`) - Планирование задач

---

## 🐛 Troubleshooting

### Backend не запускается

**Проблема:** `Error: connect ECONNREFUSED`
- **Решение:** Убедитесь, что MySQL запущен и доступен
- Проверьте настройки в `.env`

**Проблема:** `Error: ER_BAD_DB_ERROR: Unknown database`
- **Решение:** Создайте БД: `npm run db:create`

### Mobile не подключается к API

**Проблема:** Network error / Connection refused
- **Решение для эмулятора Android:** Используйте `10.0.2.2` вместо `localhost`
- **Решение для физического устройства:** Используйте IP-адрес вашего ПК
- Убедитесь, что firewall не блокирует порт 3000

**Проблема:** Freezed генерация не работает
- **Решение:** Запустите `flutter pub run build_runner build --delete-conflicting-outputs`

### JWT Token ошибки

**Проблема:** 401 Unauthorized
- **Решение:** Обновите токен или войдите заново
- Проверьте, что `JWT_SECRET` в `.env` корректный

---

## 📦 Структура проекта

```
RabbitFarm/
├── backend/                 # Backend API (Node.js + Express)
│   ├── src/
│   │   ├── controllers/     # 13 контроллеров
│   │   ├── models/          # 14 моделей Sequelize
│   │   ├── routes/          # 15 роутов
│   │   ├── validators/      # 10 валидаторов Joi
│   │   └── utils/
│   └── package.json
│
├── mobile/                  # Mobile App (Flutter)
│   ├── lib/
│   │   ├── core/            # API, Router, Theme
│   │   └── features/        # 15 модулей
│   └── pubspec.yaml
│
├── ARCHITECTURE.md          # Архитектура проекта
├── PROJECT_SUMMARY.md       # Полное описание
├── COMPLETION_REPORT.md     # Отчет о завершении
└── QUICKSTART.md           # Этот файл
```

---

## 🎉 Готово!

Теперь у вас запущена полная система управления кролиководческой фермой:

- ✅ Backend API на http://localhost:3000
- ✅ Mobile приложение на вашем устройстве
- ✅ База данных MySQL с 14 таблицами
- ✅ 15 полнофункциональных модулей
- ✅ Dashboard с аналитикой

**Следующие шаги:**
1. Зарегистрируйте пользователя
2. Откройте Dashboard
3. Начните добавлять данные (породы → клетки → кроликов)
4. Используйте все функции системы!

---

**Нужна помощь?**
- Изучите [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) для детального описания
- Посмотрите [ARCHITECTURE.md](ARCHITECTURE.md) для понимания архитектуры
- Проверьте [COMPLETION_REPORT.md](COMPLETION_REPORT.md) для списка всех возможностей
