# 🚀 Быстрый старт RabbitFarm

## Статус проекта: ✅ Готов к использованию

---

## Backend (уже запущен)

Сервер работает на: **http://localhost:3000**

### Проверка работоспособности:
```bash
# Проверить здоровье сервера
curl http://localhost:3000/health

# Посмотреть доступные endpoints
curl http://localhost:3000/api/v1
```

### Если нужно перезапустить:
```bash
cd backend
npm run dev
```

---

## Mobile (Flutter)

### 1. Установить зависимости:
```bash
cd mobile
flutter pub get
```

### 2. Сгенерировать код (Freezed):
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. Настроить API endpoint:

Откройте `mobile/lib/core/api/api_endpoints.dart` и установите правильный baseUrl:

```dart
// Для Android эмулятора:
static const String baseUrl = 'http://10.0.2.2:3000/api/v1';

// Для физического устройства (замените на IP вашего ПК):
static const String baseUrl = 'http://192.168.1.XXX:3000/api/v1';

// Для iOS симулятора или веб:
static const String baseUrl = 'http://localhost:3000/api/v1';
```

### 4. Запустить приложение:
```bash
flutter run
```

---

## Первый вход

### Регистрация нового пользователя:

1. Откройте приложение
2. Нажмите "Регистрация"
3. Заполните форму:
   - Email: `admin@rabbitfarm.com`
   - Пароль: `password123`
   - Имя: `Администратор`
   - Роль: `owner`

### Или используйте тестовый аккаунт (если есть seed данные):
- Email: `test@example.com`
- Пароль: `password`

---

## Основные функции

### ✅ Реализовано:

1. **Кролики** - полное управление (CRUD, фото, вес, родословная)
2. **Породы** - справочник пород
3. **Клетки** - управление клетками и размещением
4. **Случки** - планирование и учет случек (НОВОЕ!)
5. **Окролы** - регистрация рождений
6. **Вакцинация** - график прививок
7. **Медицинские карты** - история болезней
8. **Корма** - складской учет
9. **Кормление** - записи кормления
10. **Финансы** - доходы и расходы
11. **Задачи** - планирование работ
12. **Отчеты** - дашборд и аналитика

---

## Навигация в приложении

### Нижнее меню:
- 📅 **Сегодня** - задачи на сегодня
- 📊 **Обзор** - дашборд с аналитикой
- 🐰 **Кролики** - список кроликов
- ✅ **Задачи** - список задач
- ⋯ **Ещё** - дополнительные функции

### Меню "Ещё":
- Клетки
- Породы
- Случки (НОВОЕ!)
- Окролы
- Вакцинация
- Медицинские карты
- Корма
- Кормление
- Финансы
- Отчеты

---

## Модуль "Случки" (Breeding)

### Доступные экраны:

1. **Планировщик случек** (`/breeding/planner`)
   - Выбор самца и самки
   - Анализ инбридинга
   - Оценка рисков родственного скрещивания
   - Рекомендации

2. **Список случек** (`/breeding`)
   - Все зарегистрированные случки
   - Фильтрация по статусу
   - Карточки с информацией о родителях

3. **Создание случки** (`/breeding/new`)
   - Форма регистрации новой случки
   - (В разработке - placeholder)

4. **Детали случки** (`/breeding/:id`)
   - Подробная информация
   - (В разработке - placeholder)

### API endpoints:
```
GET    /api/v1/breeding              - список случек
GET    /api/v1/breeding/statistics   - статистика
GET    /api/v1/breeding/:id          - детали
POST   /api/v1/breeding              - создать
PUT    /api/v1/breeding/:id          - обновить
DELETE /api/v1/breeding/:id          - удалить
```

---

## Структура проекта

```
RabbitFarm/
├── backend/                 # Node.js API
│   ├── src/
│   │   ├── controllers/    # 13 контроллеров (включая breeding)
│   │   ├── models/         # 14 моделей БД
│   │   ├── routes/         # 14 роутов
│   │   ├── services/       # 4 сервиса
│   │   └── validators/     # 10 валидаторов
│   └── migrations/         # Миграции БД
│
├── mobile/                  # Flutter приложение
│   └── lib/
│       ├── core/           # API, роутинг, темы
│       ├── features/       # 12 модулей
│       │   ├── auth/
│       │   ├── rabbits/
│       │   ├── breeding/   # НОВЫЙ МОДУЛЬ
│       │   ├── cages/
│       │   ├── health/
│       │   ├── feeding/
│       │   ├── finance/
│       │   ├── tasks/
│       │   └── reports/
│       └── shared/         # Общие виджеты
│
└── docs/                    # Документация
```

---

## Troubleshooting

### Backend не запускается:
```bash
# Проверить переменные окружения
cat backend/.env

# Проверить подключение к БД
mysql -u root -p
USE rabbitfarm;
SHOW TABLES;
```

### Mobile не компилируется:
```bash
# Очистить кеш
flutter clean
flutter pub get

# Пересоздать generated файлы
flutter pub run build_runner clean
flutter pub run build_runner build --delete-conflicting-outputs
```

### Ошибка подключения к API:
1. Проверьте, что backend запущен
2. Проверьте baseUrl в `api_endpoints.dart`
3. Для Android эмулятора используйте `10.0.2.2` вместо `localhost`

---

## Что дальше?

1. **Заполнить тестовыми данными** - добавить кроликов, клетки, породы
2. **Создать первую случку** - через планировщик или форму
3. **Зарегистрировать окрол** - после успешной случки
4. **Настроить вакцинацию** - добавить график прививок
5. **Вести финансовый учет** - записывать доходы и расходы

---

## Полезные ссылки

- **Backend API**: http://localhost:3000/api/v1
- **Health Check**: http://localhost:3000/health
- **Документация**: См. файлы в папке `docs/`

---

**Приятного использования! 🐰**
